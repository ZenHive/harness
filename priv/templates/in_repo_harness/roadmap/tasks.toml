schema_version = 2
project = "app"
default_branch = "main"
vision = "Ship the target project with harness-driven agent dispatch."

[phases.1]
name = "Bootstrap"
order = 1
status = "in_progress"

[bundles.bootstrap]
description = "Initial harness setup"
order = 1
phase = 1

[[task]]
id = "1"
phase = 1
bundle = "bootstrap"
status = "pending"
title = "First harness task"
scores = { d = 2, b = 5, u = 5 }
acceptance_criteria = ["Replace this placeholder with a real task"]
body = "Edit this roadmap entry once the in-repo harness subdirectory is wired. Run `rmap next` from the target repo root or ingest via IEx."
created_at = "2026-05-27"
