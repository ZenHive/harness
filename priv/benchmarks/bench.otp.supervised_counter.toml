id = "bench.otp.supervised_counter"
version = 1
domains = ["otp", "genserver"]
expected_green = true

[task]
intent = "Add Harness.Benchmark.Eval.Otp.SupervisedCounter — a Supervisor with one Counter GenServer child that increments via call and restarts on crash."
acceptance = [
  "Supervisor starts a registered counter child",
  "increment/1 returns the new count",
  "child restarts with a fresh count after kill"
]

[target]
project = "harness"
check_stack = "elixir"
