id = "bench.elixir.tree_walk"
version = 1
domains = ["elixir"]
expected_green = true

[task]
intent = "Add Harness.Benchmark.Eval.Elixir.TreeWalk — pure depth-first walk over %{value, children: list} nodes returning values in DFS pre-order."
acceptance = [
  "walk/1 returns [] for nil",
  "walk/1 visits parent before children",
  "walk/1 handles nested children"
]

[target]
project = "harness"
check_stack = "elixir"
