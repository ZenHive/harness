id = "bench.ecto.embedded_profile"
version = 1
domains = ["ecto"]
expected_green = true

[task]
intent = "Add Harness.Benchmark.Eval.Ecto.EmbeddedProfile — an embedded_schema for profile (name, age) with a changeset that requires name and age 1..150."
acceptance = [
  "embedded_schema defines :name and :age fields",
  "changeset/2 casts and validates required name",
  "changeset/2 validates age is between 1 and 150"
]

[target]
project = "harness"
check_stack = "elixir"
