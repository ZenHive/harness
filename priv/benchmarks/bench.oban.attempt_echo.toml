id = "bench.oban.attempt_echo"
version = 1
domains = ["oban"]
expected_green = true

[task]
intent = "Add Harness.Benchmark.Eval.Oban.AttemptEcho — an Oban.Worker that returns {:ok, %{attempt: job.attempt}} from perform/1."
acceptance = [
  "perform/1 reads job.attempt",
  "returns {:ok, map with :attempt key}",
  "module declares Oban.Worker"
]

[target]
project = "harness"
check_stack = "elixir"
