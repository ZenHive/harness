# harness — CLAUDE.md

**Repo:** [github.com/ZenHive/harness](https://github.com/ZenHive/harness) (public, default branch `development`).

@~/.claude/includes/across-instances.md
@~/.claude/includes/critical-rules.md
@~/.claude/includes/worktree-workflow.md
@~/.claude/includes/task-prioritization.md
@~/.claude/includes/task-writing.md
@~/.claude/includes/rmap.md
@~/.claude/includes/workflow-philosophy.md
@~/.claude/includes/ex-unit-json.md
@~/.claude/includes/dialyzer-json.md
@~/.claude/includes/code-style.md
@~/.claude/includes/development-commands.md
@~/.claude/includes/development-philosophy.md

Situational topics covered by trigger-loading skills (no `@`-import — invoke via the Skill tool when the trigger phrasing matches; they do not auto-load at session start): `elixir:reach` (PDG/SDG, `mix reach.otp` OTP introspection), `elixir:web-command` (browser interaction, dashboard work post-Task-50), `elixir:agent-economy` (descripex consumer surface, if/when harness adopts it), `elixir:elixir-setup` (dep stack changes, alias edits).

**AI driver surface (canonical for orchestrators):** @skills/harness-driver/SKILL.md — load this when you are the consumer driving harness (the primary user). It is the stable contract for delegation patterns, non-delegatable handling, result interpretation, and sharp edges. Any change to the public driver surfaces must update it.

## What This Is

`harness` is an OTP-native Elixir engine an **AI orchestrator drives end to end**: it pulls a task from the rmap roadmap, dispatches it to a **headless coding agent** — Claude Code, Cursor, Codex, Grok, Antigravity, Pi — running in an isolated git worktree, runs the target project's own check stack against the result, and reports a *verified* outcome back to its consumer. Today the consumer surface is an Elixir API (called from IEx, tidewave, or another BEAM process); the **Phase 7 multi-project pivot** (Tasks 44–51, milestone v0_5) reshapes it into a long-running OTP node with a **Phoenix LiveView dashboard** that orchestrates **N registered target projects** (Elixir, Rust, anything-with-a-shell-driven check stack) concurrently. MCP/JSON-CLI surface (Task 17) is **deferred** to Phase 6 — it earns its complexity only if a non-Elixir consumer (Python, TS) ever needs to drive harness from outside the BEAM, which isn't the current need.

The primary user is an AI agent, not a human — harness is the OTP-native automation of the delegate → verify → repair loop this repo already runs by hand via the `cloud-delegation` skills. For the Elixir-native consumer (Claude Code running in IEx / tidewave), the library + dashboard IS the agent surface; no JSON-RPC transport is needed.

It is not a wrapper around one agent. The `AgentAdapter` behaviour (Phase 1, Task 3) is the contract, but a deliberately thin one — it *invokes* an agent and *captures its raw output*, nothing more. There is **no normalized event model**: the consumer is an AI that reads each agent's raw JSON natively, and harness decides "did the job succeed?" from its **own verification stack**, never from the agent's self-reported result.

## Architecture

- **Elixir / OTP, not TypeScript.** The harness *is* N concurrent supervised agent runs that need crash isolation, timeouts, retries, and observable state — exactly what OTP provides. One run = one supervised `gen_statem`; one batch = a `DynamicSupervisor`. Building in TS would mean re-implementing supervision by hand.
- **The core loop.** rmap task in → dispatch to a headless agent in an isolated worktree → run the target project's check stack against the result → green ⇒ done, red ⇒ report (later: feed failures back and repair). The verification stack — not the agent — is the grader, which keeps implementer and evaluator separate (global `CLAUDE.md` § Evaluator Separation).
- **Thin adapter pattern.** One adapter per agent — invocation + raw capture + a capability declaration, nothing else. The behaviour is `Harness.AgentAdapter`: five callbacks (`capabilities/0` + `rule_channel/0` + `build_command/1` required; `classify_message/2` and `terminate/1` have defaults via `use Harness.AgentAdapter` + defoverridable), with `AgentAdapter.invoke/2` doing the generic Port spawn after attaching harness-owned rules. `build_command/1` threads caller-controlled env (Task 25 — `Invocation.env` carries set/scrub pairs through to the Port); the harness-owned rule set is delivered ahead of `build_command/1` by the behaviour-level `AgentAdapter.attach_rules/2` helper (Task 39), which dispatches on the adapter's `c:rule_channel/0` (`:system_prompt_file` for Claude, `:codex_ephemeral_file` for Codex/Pi, `:cursor_ephemeral_file` for Cursor, `:prompt_preamble` for Grok/Antigravity, `:none` for test doubles) and writes ephemeral rule files into the run worktree or augments the task prompt accordingly. Adapters shell out to each agent's headless mode over uniform OTP Ports — no per-agent SDK (see § "Orchestration Library" for why). Every adapter must pass the shared conformance suite (`Harness.AgentAdapter.ConformanceCase`) *unchanged* — a leak gets fixed in the behaviour, not patched around in the adapter.
- **No agent-output parsing.** Raw passthrough is simpler *and* more robust: each agent ships 40+ releases, so a JSON-format change is absorbed by the AI reading the transcript, not by breaking a harness normalization layer.
- **Path discipline** (see global `CLAUDE.md` § Architecture Drives Design): raw-output capture is hot-path-adjacent — keep it allocation-light; run/batch lifecycle is warm-path OTP state; the dashboard / MCP surface is cold-path.
- **Multi-project federation (Phase 7, shipped — milestone v0_5 complete, Tasks 44–51).** The implicit "harness checkout IS the project it's operating on" assumption is gone. A first-class `%Harness.Project{}` (Task 46) carries `source` (local dir or GitHub URL — Task 47), a declarative `%Harness.CheckStack{}` (Task 44; Elixir + Rust presets — Tasks 44/45), `roadmap_path`, and `concurrency_cap`. A `Harness.ProjectRegistry` GenServer holds them. **Oban** (Task 48) is the dispatch layer — queue-per-project gives both per-project concurrency caps and restart-resilience (BEAM node death no longer loses in-flight runs; jobs survive in Postgres). The custom Task-10 retry policy stays inside the `Harness.Run` gen_statem for in-run repair-loop attempts; Oban handles dispatch-level transient failures (quota) via `{:snooze, _}`. Two layers, distinct concerns. **`Oban.Plugins.Cron`** (Task 51) makes "while I sleep, harness drives the roadmap" trivial. The **Phoenix LiveView dashboard** (Task 50) is the primary cold-path consumer surface — `Harness.Dashboard.Endpoint` standalone Bandit on 4018 (conditional behind `:dashboard, :enabled` + `Code.ensure_loaded?(Bandit)` so mountable consumers aren't forced into a second HTTP server), Oban Web mounted at `/harness/oban`, custom `Harness.Dashboard.Live` for project switcher + per-bucket counts + per-run drill-down with a live transcript pane (`Phoenix.PubSub` topic `harness:run:<id>:transcript` fed by a new `:on_output` callback on `AgentAdapter.Driver.run/3`, 200 KiB bounded buffer). Tidewave's MCP plug is mounted on the same endpoint in dev (`if Mix.env() == :dev, do: plug Tidewave`) — one port (4018) serves dashboard + Oban Web + MCP.

## Orchestration Library — Build a Thin Core (settled, Phase 1 Task 2)

With the normalized event model cut, the harness core is textbook OTP — a Port per run, a `gen_statem` per run, a `DynamicSupervisor` for batches. Adopting a niche orchestration library to provide that adds risk, not leverage. Task 2 evaluated the candidates (`opal`, `gen_agent` + `gen_agent_ensemble`, `altar_ai`, `ex_mcp`) and the per-agent SDKs (`claude_code`, `codex_sdk`) — outcome **build thin core, adopt none, uniform Ports**. Full rationale + borrowed design ideas: `docs/orchestration-library-evaluation.md`.

- **None of the four orchestration libraries spawns or supervises an external OS process** — there is no Port in any of them. They *implement* or *coordinate* in-process LLM agents; harness *orchestrates external* headless CLIs. Orthogonal problem. Three of the five candidates are single-release, 0–4-star packages — niche, low-download, **do not add any as a dependency** (global `CLAUDE.md` § "Verify External Code Reviews for Correctness").
- **`claude_code` and `codex_sdk` are both CLI wrappers**, not native reimplementations — they still require (`codex`) or auto-install + spawn (`claude`) the external binary. An SDK's headline contribution is a normalized event model over the agent's output; harness's raw-passthrough design (§ "No agent-output parsing") deliberately discards exactly that, and OTP Ports already spawn the binary with the termination + idle-stream signals harness needs. So **uniform Ports for every adapter** — one invocation strategy behind the behaviour, not two.
- **Cold-path surface: Phoenix LiveView dashboard + embedded Oban Web (primary); hand-rolled JSON-RPC endpoint deferred.** The Phase 7 pivot (Task 50, milestone v0_5) makes the dashboard the primary cold-path consumer surface — custom `Harness.Dashboard.Live` for project switcher / live multi-project status / per-run drill-down with stream-json transcript pane, plus Oban Web mounted at `/harness/oban` for queue-centric introspection (job rows, retries, scheduled work). Mountable into a consumer Phoenix endpoint (pattern: Oban Web, LiveDashboard, Tidewave) or standalone via supervised Bandit. The MCP/JSON-CLI cold-path surface (Task 17) is **deferred to Phase 6** — it earns its complexity only when a non-Elixir consumer (Python, TypeScript) needs to drive harness from outside the BEAM. For Claude Code running in IEx + tidewave (the actual primary consumer), the library + dashboard IS the agent surface; no JSON-RPC transport is needed. If/when Task 17 is revisited: `descripex` (already in the dep stack) generates the tool list via `Descripex.MCP.tools/1`; what remains is the transport/handshake half — a bounded JSON-RPC-over-stdio-or-Plug endpoint, cheaper to own than to version-track a dep on a consumer-facing surface.
- **Oban as queue + persistence + cron, not as a worker engine.** Adopted in Phase 7 (Task 48). Postgres is already running on the dev laptop; Oban's queue-per-project model maps cleanly to multi-project orchestration; restart resilience matters when batches are long-running. Oban *wraps* the existing `Harness.Run` gen_statem rather than replacing it — a `Harness.Run.Worker` (`Oban.Worker`) takes a `{project_name, item_id, adapter_module}` payload, spawns the gen_statem, and threads its terminal state into Oban's return contract (`:ok` / `{:snooze, _}` / `{:cancel, _}`). The gen_statem stays load-bearing because runs are minutes-to-hours with rich live state (stream-json transcript pane in the dashboard), which is too heavy for a flat job row.
- **Dispatch retry vs. in-run retry.** Oban owns dispatch-level persistence and retry: a worker maps quota or transient terminal failures to `{:snooze, _}`, so the persisted job row survives restart and runs again later. The Task-10 retry policy inside `Harness.Run` still owns the repair loop after a red verification verdict. Keep those layers separate. With open-source Oban there is no cross-queue global cap; the local effective ceiling is the sum of all `project_<name>` queue limits, so choose project `concurrency_cap`s that add up to the laptop's capacity. Oban Pro's Smart Engine can supply a true global cap later, but it is optional.
- **`Harness.AgentRegistry` is a soft hint, not a contract** (Task 40, resolved 2026-05-27 as option (b)). Unavailability state (quota-exhausted, manually marked) lives in `GenServer` state only — there is no persistence, no TTL, no replication, and a restart clears the table by design. The registry is a *latency optimization* (skip a known-bad adapter at dispatch time so we do not burn the first attempt); *correctness* lives one layer below in Oban (workers map quota → `{:snooze, _}`, the persisted job row survives both restarts and quota windows). Quota itself is transient (≈ 5h rolling for the major providers), so persisting an unavailability mark across a restart would usually be wrong by the time the BEAM comes back up. The bounded cost of a restart-clear is one wasted run-attempt per previously-marked-unavailable adapter per restart; Oban's retry absorbs the rest. Revisit only as **option (c) — TTL** (matching the provider's quota window) if harness ever runs as a long-uptime daemon; never as **option (a) — persistence without a TTL** (buggy by construction). Full rationale in `lib/harness/agent_registry.ex` `@moduledoc` § "Availability is a soft hint, not a contract".

## Agent Headless Entry Points (domain reference)

| Agent | Headless invocation | Raw output format |
|---|---|---|
| Claude Code | `claude -p` | `--output-format stream-json` |
| Cursor | `cursor-agent -p` | `--output-format stream-json` |
| Codex | `codex exec` | `--json` |
| Grok | `grok -p` / the `agent` subcommand | `--output-format streaming-json` |
| Antigravity | `agy -p` | none (plain text) |
| Pi (pi.dev) | `pi -p` | `--mode json` |

All six are driven over OTP Ports — uniform invocation strategy, no per-agent SDK (see § "Orchestration Library").

Non-delegatable adapters (such as `Grok`, `Antigravity`, and `Pi`) run with a `claude`, `codex`, or `cursor`-rendered prompt. They cannot be target arguments to `Harness.Roadmap.ingest/2` since `rmap delegate --to` does not support them. Instead, ingest the task with one of the delegatable agents, and pass the resulting `%Harness.Roadmap.Item{}` directly to the non-delegatable adapter's module via `Harness.Run.Supervisor.start_run/4` (or via `Harness.Batch` using the adapter module).

**Accepted as dogfood drivers** via the two-step ingest-then-adapter pattern above: `Grok` is accepted now (Round-1 Task 25 was Grok-driven, settled `:passed`); `Antigravity` will be accepted once Task 32 resolves (`agy` currently ignores its Port `cwd` and edits the main checkout instead of its run worktree, so an Antigravity dogfood run mutates the live repo). Dispatch friction (two steps instead of one) is not a reason to silently exclude them from a parallel batch — rotate them in deliberately for adapter coverage.

harness captures these **raw** and passes them through — it does not parse or normalize them. Known gotcha: the headless **exit code is unreliable**. Derive *termination* from the process / Port closing plus a timeout guard; derive *success* from harness's own verification stack — never from `$?`, never from the agent's self-reported result.

## Reach Is in the Dep Stack

The harness core is unusually OTP-dense — supervision trees, a per-run `gen_statem`/GenServer, a `DynamicSupervisor` batch layer, and independence reasoning when deriving batches. `mix reach.otp` (state-machine analysis, dead replies, missing handlers, supervision topology) and `Reach.independent?` are directly on-point. Reach is a dev/test analysis dependency (`runtime: false`, not shipped); invoke the `elixir:reach` skill when doing OTP introspection or static analysis here.

## Status

Tasks 1–8 are done — OTP scaffold, the `AgentAdapter` behaviour, the Claude Code adapter, worktree-per-job lifecycle, rmap task ingestion, the verification runner, and the supervised run lifecycle. The dogfooding bootstrap is also complete: Task 24 (commit the agent's work to the run branch before teardown) and Task 23 (immediate-EOF stdin for Port-spawned agents) are done — Task 23, plus a verification-preset fix, were surfaced by the first dogfood run itself (see § Dogfooding below). Task 12 — the reusable adapter conformance suite (`Harness.AgentAdapter.ConformanceCase`), the gate every adapter is held to — is also done. Tasks 14, 13 and 15 — the Codex, Cursor and Grok headless adapters — were delivered by the **first parallel dogfood batch**: three rmap tasks dispatched concurrently through harness, each built by a headless agent in its own worktree and verified green by the check stack. The Phase 3 resilience bundle — Task 9 (batch orchestrator with a concurrency cap), Task 10 (failure-classified retry policy), Task 11 (autonomous repair loop: a red verdict is fed back to the same agent via session resume and re-graded, up to `:max_repair_attempts`) — was delivered by a **parallel multi-agent dogfood batch**, the three tasks dispatched concurrently and driven by Codex, Cursor and Claude respectively — the first dogfood batch to drive non-Claude adapters. The Phase 4 multi-agent bundle is now in progress: a **Round-1 multi-agent dogfood batch** delivered Task 25 (caller-controlled agent environment on the `AgentAdapter` contract, driven by Grok) and Task 31 (the formalized non-delegatable-adapter contract, driven by Antigravity) — the first batch to drive Grok and Antigravity as agents. The Antigravity run surfaced a worktree-isolation bug — `agy` ignores its Port cwd and edits the main checkout rather than its run worktree — filed as Task 32. A **Round-2 parallel multi-agent dogfood batch** then delivered Task 22 (harness-owned rule set injected into every adapter's invocation, driven by Cursor) and Task 16 (capability + availability registry with quota fail-over, driven by Codex) — both runs went green after a single repair-loop cycle, the first time the autonomous repair loop did genuine work on a real task, and both closeouts dogfooded rmap Task 28's new outcome layer (`--delivered-by`, `--verified` recorded against the `done` transition). A **Round-3a parallel multi-agent dogfood batch** then delivered Task 34 (audit-surfaced `Batch.fill_slots` crash on `AgentRegistry` exhaustion, driven by Claude as cross-agent fix of Codex's Round-2 Task 16 code), Task 18 (`Harness.StatusView` + `mix harness.status`, driven by Cursor), and Task 19 (structured run logging + pluggable result store, driven by Codex). 34 and 18 settled `:passed` cleanly via the verification stack; 19 surfaced a Codex regression — the worktree was empty at agent startup and missing at finalize, Codex fell back to editing the main checkout, harness's `git add` then crashed and the run settled `:commit_failed`. Codex's implementation was real and self-graded green, so the diff was hand-integrated onto `development` (conflicts in `batch.ex` / `batch_test.exs` against Task 34 resolved manually) and re-verified by the same stack post-integration. The Codex worktree-isolation regression was filed as Task 41 (mirrors Task 32, the Antigravity equivalent). A **Round-3b parallel multi-agent dogfood batch** then delivered Task 30 (pin `Worktree.commit/2` to the run's `harness/<id>` branch, driven by Claude), Task 35 (audit-surfaced `Batch.fill_slots` race — `start_run` errors crash the batch, driven by Cursor), Task 38 (audit-surfaced batch slot held until `terminal_linger` expires, driven by Codex), Task 29 (force-settle the Run lifetime timeout when the agent handle never arrives, driven by Claude), and Task 37 (audit-surfaced repair loop ignoring quota classification before resuming, driven by Codex) — all five settled `:passed`, the first five-up batch to fan out cleanly. A green-but-noisy verification side effect — credo's `TagTODO` check firing on inherited pre-existing `TODO:` comments — was filed as Task 43. A **Round-4 parallel multi-agent dogfood batch** then delivered Task 27 (hoist universal adapter callbacks into the `AgentAdapter` behaviour via `use … + defoverridable`, driven by Grok), Task 28 (wire the failure-classified retry policy into the batch orchestrator, driven by Cursor), Task 36 (audit-surfaced harness-injected rule files being committed by `Worktree.commit/2`, driven by Codex), and Task 43 (diff-aware credo `TagTODO` baseline filter that drops findings on inherited debt, driven by Claude). 27 and 28 settled `:passed` cleanly; 36 and 43 went `:verification_red` on verification-stack false positives (sobelow's `Traversal.FileModule` on harness-controlled internal paths; dialyzer not knowing about the first `lib/` use of `Jason.*` under `plt_add_deps: :apps_direct`). Both diffs were correct and were hand-integrated, with the verification preset tightened in the same closeout commit (`sobelow_skip` annotations + `:jason` added to `plt_add_apps`); the harness verification preset (not just the dispatched code) was the load-bearing fix. A **Round-5 parallel multi-agent dogfood batch** dispatched 5 tasks (32, 33, 39, 41, 52) across Claude, Cursor and Grok; three settled `:passed` — Task 39 (hoist rule injection into the `AgentAdapter` behaviour — new required `rule_channel/0` callback enumerating 5 delivery channels, `attach_rules/2` + `task_prompt/1` helpers in the behaviour, all 5 adapters refactored to declare their channel, driven by Cursor), Task 32 (declare Antigravity `worktree_isolation: false` + new `Harness.Worktree.Isolation` module that rejects dispatch up front and snapshots the main checkout porcelain for pollution detection mid-run, driven by Cursor), and Task 52 (`Harness.AgentAdapter.Pi` headless adapter — `pi -p --mode json`, codex-style AGENTS.md auto-discovery, `--continue` for session resume, driven by Claude). Tasks 33 (Same-task A/B agent-evaluation mode) and 41 (Codex worktree-isolation regression) orphaned when the dogfood driver BEAM exited after 5 receive iterations (3 settlements + 2 normal DOWN messages from the same settled tasks); both rolled back to `pending` for re-dispatch. Round-5 surfaced the first cross-task design overlap of the project: Tasks 32 and 39 both modified `lib/harness/agent_adapter.ex` and `lib/harness/agent_adapter/antigravity.ex` against a pre-batch development base, and the cherry-picks composed cleanly via three-way merge because each branch added disjoint regions of the same file — first proof that the Round-5-sized fan-out is integration-cost-tractable when the agents work on overlapping surfaces. Task 52's pi.ex was authored against pre-39 development and required a small post-cherry-pick contract patch (`rule_channel: :codex_ephemeral_file` + `attach_rules`/`task_prompt` wiring) to satisfy the new required-callback gate; landed in the same closeout commit alongside two `@moduledoc false` → real-moduledoc touches on `RuleDelivery` and `Git` whose types had begun leaking through public `@spec`s. Task 58 (`Harness.AuditReview` — synchronous HIGH-tier audit grader dispatch wrapper for the codified `staged-review:audit-review` skill) was then hand-built on the agent-facing surface line (milestone v0_4): `grade_fix/1` dispatches the opposite-agent grader (`:claude ↔ :codex` auto-pair) via `Harness.AgentAdapter.Driver.run/3` directly — bypassing `Harness.Run` and the verification stack on purpose (the grader's *text* IS the verdict, not a green/red check; routing through verification would require a fake passthrough check and pollute the result store with non-task runs) — and parses a `<<<VERDICT:APPROVE|REJECT>>>` sentinel with last-match-wins, returning `{:ok, %{verdict, outcome, grader}}` for any dispatch that spawned.

The **Phase 7 multi-project + dashboard pivot** (milestone v0_5, 8 tasks 44–51) is now **8/8 done — Phase 7 complete** — hand-built in the Claude Code session per § Dogfooding "Phase 7 is a hand-built exception" (the bundle reshapes the Application supervision tree, dep stack, and verification preset, which the grader can't dogfood while it's changing shape). Tasks 44 (`%Harness.CheckStack{}` abstraction + Elixir preset extracted), 45 (Rust preset — `cargo fmt --check`, `cargo clippy`, `cargo test`), 46 (`%Harness.Project{}` + `Harness.ProjectRegistry` GenServer), 48 (Oban dispatch with Postgres-backed restart persistence — queue-per-project, `Harness.Run.Worker` wrapping the existing gen_statem) all landed first. Tasks 47 (GitHub project sources — `{:github, url}` clone-and-cache via `Harness.Project.Source.Github`, `git fetch` + fast-forward before each run, transparent re-clone on cache loss), 49 (in-repo `<repo>/harness/` subdirectory recipe + `Harness.InRepoTemplate` reading from `priv/templates/in_repo_harness/`), and 51 (`Oban.Plugins.Cron`-driven autonomous roadmap polling via `Harness.Cron.RoadmapPoller` — queue-headroom + AgentRegistry-gated dispatch, disabled by default) landed in the next wave — each cherry-picked from a per-task delivery commit then trimmed for verification-preset edge cases (Task 47: ExDoc `t:` autolink fixes on opaque-type references; Task 49: `@doc` for doctor + comment-form sobelow_skip on `File.cp_r!`; Task 51: `apply/2` restored on `Oban.Cron.Expression.next_at/2` because dialyzer can't pierce Oban's opaque expression type, plus an unreachable adapter-clause delete). Task 50 (Phoenix LiveView dashboard + embedded Oban Web) closes the milestone: `Harness.Dashboard.Endpoint` (standalone Bandit on 4018, conditional in the Application supervision tree behind `:dashboard, :enabled` + `Code.ensure_loaded?(Bandit)`), `Harness.Dashboard.Router` (`:browser` pipeline mounting `oban_dashboard("/harness/oban", oban_name: Harness.Oban)` from `oban_web`, then `live("/harness", ..., as: :dashboard)` and `live("/harness/runs/:run_id", ..., as: :dashboard)`), and `Harness.Dashboard.Live` (project switcher off `ProjectRegistry.list/0`, per-bucket counts off `StatusView.snapshot/0` + `classify/1`, 1s `:tick` poll, per-run drill-down off `Harness.Run.status/1`). Pass 2 wires a live transcript pane: `Harness.Dashboard.Transcript.broadcast/2` over `Phoenix.PubSub` topic `harness:run:<id>:transcript`, fed by a new `:on_output` callback on `Harness.AgentAdapter.Driver.run/3` (every `{:output, data, next_run}` chunk in `loop/7`), with the LiveView holding a 200 KiB bounded transcript buffer. Bandit flipped `optional: true` so mountable consumers with their own Phoenix endpoint do not get a second HTTP server forced into their supervision tree. Browser-verified end-to-end (HTTP 200 on `/harness` + `/harness/oban`, Oban Web full UI mounted under the harness router). Task 17 (MCP/JSON-CLI surface) reclassified from Phase 5 to Phase 6 (Deferred) — the dashboard supersedes its primary use case for Elixir-native consumers; MCP stays a future idea for non-Elixir consumers. `ROADMAP.md` (rendered from `roadmap/tasks.toml` by `rmap`) is the source of truth for what to build next: start with `rmap next`.

## Dogfooding — harness Builds harness

From the core loop onward, harness is developed *by* harness. Tasks 8, 24 and 23 are the hand-built bootstrap — the supervised run lifecycle, committing the agent's work to the run branch before teardown, and giving Port-spawned agents an immediate-EOF stdin (without which a headless agent stalls peeking stdin and exits doing nothing). They and everything before them are hand-built in a Claude Code session. **Once the bootstrap is `done`, every remaining pending task is delivered by dispatching it through harness itself** — harness ingests the rmap task, runs a headless agent in an isolated worktree, and grades the result against the verification stack. Do not hand-build a dogfoodable task in a Claude Code session; hand-build only what harness cannot yet do for itself.

- **Cutover is the bootstrap (Tasks 8, 24, 23), derivable from rmap.** Once all are `done`, the next `rmap next` task is a dogfood candidate — drive it via harness, don't implement it directly.
- **Resilience landed (Tasks 9, 10, 11).** A red verdict is no longer a stop-the-line event — harness resumes the same agent with the failing checks fed back and re-grades, up to `:max_repair_attempts`, before settling `:failed`. With the batch orchestrator (Task 9) and failure-classified retry policy (Task 10) also landed, an `rmap next-bundle` can be fanned out and dogfooded unattended in one batch under a concurrency cap — a human no longer has to read every red verdict and re-dispatch.
- **The roadmap is harness's own test corpus.** Every dogfooded task doubles as a live integration test — a task harness fails to deliver is a harness bug, filed via `rmap new`, not worked around by falling back to hand-building.
- **Verification stays separate.** The dispatched agent is the implementer; harness's own check stack is the grader (global `CLAUDE.md` § Evaluator Separation). A dogfooded task is done when the verification stack is green — never on the agent's self-reported result.
- **Operational runbook:** `docs/dogfooding-workflow.md` — the concrete steps (driver script, verdict handling, known sharp edges) for dispatching a task through harness. This section is the policy; that file is the how.
- **AI driver skill (primary consumer reference):** `skills/harness-driver/SKILL.md` — the terse, pattern-oriented guide an AI orchestrator loads to know the current recommended surfaces, the two-step non-delegatable pattern, how to read results, and when to stay inside harness vs hand-build. **This is the canonical reference for the primary user.** Changes to `Harness.AgentAdapter.*`, `Harness.Run.Supervisor`, `Harness.Batch`, `Harness.Roadmap`, or the Invocation/result shapes must update it (see the DRIVER-SURFACE markers inside the skill). Treat it as load-bearing documentation, not optional prose.
- **Phase 7 is a hand-built exception.** Tasks 44–51 are mostly scaffolding (CheckStack abstraction, Project registry, Oban + Postgres setup, `mix phx.new`-style Phoenix integration, GitHub clone-and-cache, LiveView dashboard, cron plugin) — load-bearing infrastructure, not application logic. Hand-build them in a Claude Code session rather than dispatching through harness. Rationale: (a) most of these tasks change the shape of harness's own runtime (Application supervision tree, dep stack, Endpoint), which is exactly the territory a headless agent has the least leverage on; (b) the verification stack itself is being reshaped by Task 44, so dogfooding while the grader changes shape is more friction than signal; (c) `mix phx.new` is the canonical bootstrap and is faster to invoke directly than to coach an agent through. Dogfooding resumes as the default once Phase 7 lands — the policy above is the steady-state rule, this is the explicit pivot-window exception.
- **Multi-project federation lands in Phase 7 (queued).** Once Task 46 (`%Harness.Project{}` + ProjectRegistry) and Task 48 (Oban-backed dispatch) land, dogfooding extends from "harness on harness" to N registered projects — each carries its own `%Harness.CheckStack{}` and `roadmap_path`. With Task 51 (`Oban.Plugins.Cron`) enabled, dogfooding can run autonomously: every N hours, harness polls each registered project's roadmap, dispatches the next eligible task, the agent runs in an isolated worktree, the project's own check stack grades, the dashboard reflects it. Until those tasks ship, dogfooding stays single-project (harness orchestrates harness) and manual-dispatch only.
