schema_version = 2
project = "empty"
default_branch = "main"
vision = "A fixture roadmap with no pending tasks — exercises the :no_pending_task path."

[phases.1]
name = "Fixture Phase"
order = 1
status = "done"

[bundles.fixture]
description = "Fixture bundle"
order = 1
phase = 1

[[task]]
id = "1"
phase = 1
bundle = "fixture"
status = "done"
title = "The only fixture task, already done"
scores = { d = 2, b = 5, u = 5 }
body = "The roadmap's only task, already done — so rmap next finds nothing pending."
implemented = "as specified in body"
created_at = "2026-05-20"
done_at = "2026-05-20"
