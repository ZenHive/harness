id = "bench.malformed"
domains = ["elixir"]
expected_green = true

[task]
intent = "This item is missing its version."
acceptance = ["Malformed items fail loudly"]

[target]
project = "harness"
check_stack = "elixir"
