id = "bench.liveview.filter"
version = 2
domains = ["liveview", "phoenix"]
expected_green = false

[task]
intent = "Exercise a LiveView benchmark fixture."
acceptance = ["The item can be filtered by LiveView domain"]

[target]
project = "harness-dashboard"
check_stack = "elixir_precommit"
