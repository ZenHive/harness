# Audit Report: 668c2e4

**Audit range (landed, already merged to development):**  
668c2e4 (roadmap: task 255 -> done) through f56ac9c (harness: agent delivery — task 258 Reviewer-reliability + facet SQL fast paths) inclusive.

**Commits in the audited batch:**
- 668c2e4 roadmap: task 255 -> done (shipped d8261e4534cc)
- d8261e4 harness: agent delivery — task 255 Per-role reviewer model — split the reviewer model from the implementer's per-agent default (run run-1781221243552-309c9ead)
- ffdd534 roadmap: task 258 -> done (shipped 21951d2910af)
- 21951d2 harness: reviewer fixes — task 258 Reviewer-reliability + facet SQL fast paths — finish what Task 139 started (run run-1781221243915-103c0ff1)
- f56ac9c harness: agent delivery — task 258 Reviewer-reliability + facet SQL fast paths — finish what Task 139 started (run run-1781221243915-103c0ff1)

**What was reviewed:**
- Exact diffs for the two substantive agent-delivery commits (d8261e4 + f56ac9c) plus the three roadmap/status markers via `git show`.
- Substantive surfaces: `lib/harness/config.ex` (reviewer_model_entries + reviewer_model/1 + schema wiring), `lib/harness/run.ex` (data struct + init seam for reviewer_agent_resolver, reviewer_model/1 now data-driven + calls Config.reviewer_model), `lib/harness/dashboard/settings_live.ex` (dual cards, agent_models_state vs reviewer_models_state, mount/refresh assigns, template), `lib/harness/result_store.ex` (new behaviour callbacks + public dispatch wrappers for aggregate_reviewer_reliability + aggregate_by_facet), the Memory and Postgres backends (fallback list+pure vs real grouped SQL aggregates with jsonb facet normalization), `lib/harness/dashboard/kpi_live.ex` (switched assign_facets / build_facets to the new aggregate_by_facet fast path; removal of CapabilityScore.group_by_facet + old facet_rows), plus the added parity tests (kpi_parity_test.exs, kpi_live_parity_test.exs) and memory_test updates.
- Project conventions: @spec on every new public and private function (def + defp), concise docs, tagged tuples, no bare TODO, no IO in docs, functional style, minimal diff.
- Hygiene: dead code removal, debug output, stale docs post-split, naming consistency (forms, helpers, keys), CHANGELOG completeness for the range, roadmap marker vs code separation, the supplied reviewer-quality rejection example.
- Cross-checks against current tree after laying the union of the two deliveries, plus `mix compile --warnings-as-errors`.

**What was found:**
- **Overall:** Clean landing. The per-role split (255) is narrow and correct: reviewer now has its own Config entries (still defaulting to the shared agent default for backward compat), Run threads a resolver seam (defaulting to the real AgentRegistry lookup) so tests can control reviewer→agent resolution without globals, the dashboard renders two independent cards with proper placeholder inheritance, and all call sites (invocation builder) now go through Config.reviewer_model. The 258 fast paths deliver exactly what the title promised — reviewer reliability ledger and per-facet per-agent KPI now have Postgres single-query implementations (with jsonb handling for the free-form review_facets map and rating arrays) while Memory falls back to the existing pure AgentKPI / CapabilityScore rollups for parity. KPILive no longer materializes the full record set for the facet pivot.
- **Tests:** The new parity tests are substantive, not trivial. KPIParity seeds mixed verdicts/reasons/facets/reviewers and asserts aggregate structure matches across backends. KPILiveParity boots the live view under each store and asserts the rendered HTML contains the expected reviewer reliability and facet markers. Integration-tagged as appropriate. Memory and Postgres paths are exercised.
- **Docs / comments:** Agent-models card description correctly shortened to "implementer" only; new Reviewer models card has accurate fallback language ("inherits the agent default above"). Run and Config have updated comments explaining the precedence (task pin > reviewer override > shared per-agent default > CLI ambient). No widespread stale docs left in the changed modules.
- **CHANGELOG:** Already carries the relevant entries (per-agent model work explicitly calls out Task 255 per-role reviewer override building on it; aggregate surfaces and reviewer reliability are referenced). No gap to fill for this range.
- **Conventions & hygiene nits:** Strong @spec discipline on all new code (queries, mappers like reviewer_rows_to_ledger / facet_*_to_*, safe_rate, the state helpers, reviewer_model(data)). No debug IO, no bare TODO, no # credo:disable, no commented-out paths left behind. The 21951d2 and roadmap markers touched only tasks.toml + data.json (mechanical; left untouched per audit rules).
- **One hygiene item fixed:** In the 255 delivery the agent-models form block still carried `class="reviewer-form"` (obvious copy-paste residue from the pre-split single-card era). The new reviewer-models block correctly uses the same string. Fixed forward to `class="agent-model-form"` for naming consistency with its id prefix and to distinguish the two cards.
- **Reviewer-quality feedback loop:** The supplied example (task 208 rescue-pass rejection on coverage 79.47% < 80% threshold) is outside this range (255/258). No rejections or false-rejection signals attached to the deliveries audited here.
- **Nothing else rose to a fix:** The SQL in the facet query is dense (subquery + jsonb_each/object_agg + array_agg of rating blobs) but contained, parity-tested, and matches the Memory path via the scout context builder. The reviewer_agent_resolver seam is an intentional, narrow test hook (documented by its default wiring and use only in the model resolver). Remaining aggregates (ceremony_cost, review_stuck_causes) continue to use the list+pure path — pre-existing, out of scope for the "reviewer-reliability + facet" title of 258.

**What was fixed:**
- 1-line naming hygiene in `lib/harness/dashboard/settings_live.ex`: agent-models form class renamed from "reviewer-form" to "agent-model-form" (cosmetic only; no behavior or CSS impact expected).

**Discovery filings:** none.  
No new rmap task was filed. The class nit was small enough to fix inline. The incomplete fast-path coverage on the older aggregate surfaces is real tech debt but was already present before this range and not introduced or expanded by 255/258.

**Summary for next auditor:**  
Two tightly-scoped agent deliveries (model split for per-role reviewer strength without breaking existing configs; SQL fast paths + parity for the new reviewer reliability and facet pivots). Both changes are narrow, well-tested, convention-clean, and already reflected in CHANGELOG. One trivial post-edit naming residue was the only thing that needed a forward fix. Roadmap markers were mechanical and ignored. Range is sound; the audit marker (this commit) stops here.

(End of report — commit subject is the machine-readable stop marker for the next range.)
