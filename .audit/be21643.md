# Audit report for be21643 (range: dddcab2..be21643)

**Audit agent:** post-merge hygiene pass (2026-06-15)
**Range audited (already landed on development):**
- be21643 `roadmap: task 289 -> done (shipped 55929c4cdc03)`
- 55929c4 `harness: agent delivery — task 289 Blocking multi-run await: dispatch-await_runs MCP tool (run run-1781490184500-8c5d3b99)`
- 9beb510 `dashboard(run-show): render the focused run's roadmap task details`
- dddcab2 `roadmap: task 289 -> in_progress`

**Scope:** hygiene review of dead code, stale/missing docs, CHANGELOG gaps, debug leakage, broken conventions, inconsistent naming, and general project health in the landed diff. The merge is settled; this is fix-forward only. No reverts, no blocks.

## What was reviewed
- The core delivery (55929c4): new public `Harness.Dispatch.await_runs/2` + `api(:await_runs, ...)` declaration, 7 small private helpers (`await_runs_until`, `await_run_summary`, `compact_run_summary`, `await_run_complete?`, `timeout_unfinished_run`, `wait_for_next_poll`, `not_found_summary`), the `@await_runs_poll_ms` constant, and tests in `dispatch_test.exs` + `oban_dispatch_test.exs` (settled fast-path, not-found, Oban in-flight timeout marker via the lookup seam).
- The dashboard enhancement + backfill (9beb510): `load_task_item/2` + `assign_task_item/1` in `Harness.Dashboard.Live`, the `task_section/1` HEEx function component, new `.task-*` CSS tokens in `tokens.ex`, updates to live_test.exs for the nil graceful cases, plus the accompanying ROADMAP.md + roadmap/ (tasks.toml + data.json) backfill record for task 295.
- The two pure status-marker commits (roadmap bookkeeping only).

All new `@spec`s (including on `defp`) are present. The `api/4` macro + Manifest/Chat.Tools path makes the tool appear on the MCP/JSON surface automatically (verified by the added manifest registry test). Dashboard reuses existing `.field` / `config-pill` markup and the established graceful-nil contract for roadmap reads. No new visual identity.

## Findings
1. **CHANGELOG gap (hygiene).** The Unreleased "Added" section had no entry for the new `dispatch-await_runs` blocking multi-await tool (Task 289) or for the run-show roadmap task details enrichment + task 295 backfill. Recent similar deliveries (e.g. 292/293) received explicit bullets; prior post-merge audits have performed "changelog hygiene" passes for exactly this reason.
2. **Driver surface documentation gap (hygiene — required by the file itself).** `skills/harness-driver/SKILL.md` (the canonical AI driver contract) was not updated for the new public flat tool. The file's "Anti-Staleness Contract" and "Changes that require an update to this skill" section explicitly list new public functions on `Harness.Dispatch` and changes to which `api()`-annotated functions appear in the JSON/MCP orchestrator surface. The primary surface table, the "Dispatch:" bullet under exposed JSON tools, the "Canonical loop" sentence, and the "When to use which" table all omitted `dispatch-await_runs`. (The in-process Manifest + Chat.Tools registry were already correct via the `api` declaration; only the human-readable skill lagged.)

No other hygiene issues found in the range:
- No leftover debug IO, no bare `TODO`s, no `# credo:disable` or `@tag :skip`.
- Tests are useful and targeted (exactly the ACs + manifest presence + Oban seam coverage); they are not trivial.
- Naming is consistent (`await_runs` / `dispatch-await_runs` mirrors `await` / `dispatch-await`).
- No dead code; the helpers are tight and the recursion + receive-after poll is the idiomatic bounded wait (mechanical substrate only).
- Dashboard addition follows existing patterns and the "live read + graceful nil" contract called for in its own ACs; conditional rendering per sub-block is clean.
- Roadmap status + backfill commits are minimal and follow the established harness bookkeeping shape.
- The supplied reviewer-rejection note was for task 208 (coverage 79.47% on `mix precommit.full`). It is unrelated to this range (task 289 harness agent delivery + task 295 dashboard work). The 289 delivery was reviewer-approved and landed cleanly; no false-rejection signal to note for the audited commits.

**Discovery filing:** None. No additional tech debt, orphaned paths, uncovered edges, or deferred decisions were surfaced during the review that warranted a new rmap task. (The doc gaps were real but routine hygiene; they are closed by this audit's forward fixes.)

## Fixes applied (this audit)
- Added two concise bullets under `## [Unreleased] / ### Added` in CHANGELOG.md documenting the `dispatch-await_runs` MCP tool (Task 289) and the run-show task details feature + backfill (Task 295). Style and level of detail match recent dispatch/dashboard entries and prior audit hygiene commits.
- Updated `skills/harness-driver/SKILL.md` in the required places:
  - Added a row for `dispatch-await_runs` in the Primary Surface table (immediately after `dispatch-await`).
  - Added it to the "Dispatch:" list under "Exposed JSON tools".
  - Extended the "Canonical loop" sentence to mention `dispatch-await_runs` alongside `dispatch-await`.
  - Added a row in the "When to use which" table.
  - Added a short "Blocking multi-run await — `dispatch-await_runs`" prose paragraph after the existing `dispatch-await` description (with shape, semantics, and usage guidance).

These are forward doc-only fixes. No Elixir sources, tests, or behavior were changed. The landed implementation already satisfied the reviewer gate and project checks for its delivery.

## Outcome
Range was largely clean. The two doc surfaces (CHANGELOG and the canonical driver skill) lagged the new public `Harness.Dispatch` surface; both are now current. Finding nothing would also have been a valid result — the implementation is small, well-specified, follows the project's `@spec`-everywhere + api-macro + test-seam conventions, and reuses existing dashboard idioms.

**Machine-readable summary written to** `.harness/audit.json` (not committed).

Next audit range starts after the commit created from this report.

---

**Commit subject (one commit for report + fixes):**
`audit(be21643): changelog + driver-skill hygiene for dispatch-await_runs (289) and run-show task details (295)`