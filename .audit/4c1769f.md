# Audit — range `e9f3085~1..4c1769f`

Date: 2026-06-16
Auditor: post-merge audit agent (harness)

## Commits reviewed

- `e9f3085` — task 309: expose `warm_paths` on the project-authoring surfaces (MCP `register_project/8` + dashboard form) — the only code-bearing commit.
- `7df1985` — roadmap bookkeeping (task 309 done, task 310 pending).
- `4c1769f` — roadmap bookkeeping (task 310 → in_progress).

## What I reviewed

- `lib/harness/dispatch.ex` — new trailing `warm_paths` `:value` arg (default `[]`) on `register_project`, arity bumped /7 → /8 with matching `@spec` and descripex param entry.
- `lib/harness/dashboard/settings_live.ex` — `parse_warm_paths/1` (newline/comma split, trim, reject-empty), form prefill via `Enum.join/2`, and removal of `warm_paths` from `preserved_project_attrs/1` (the form now supplies it, so preserving the stale value was dead).
- `lib/harness/dashboard/components.ex` — `warm_paths` textareas on both the edit and create project forms with aria-labels matching the surrounding pattern.
- `lib/harness/project_registry.ex` — moduledoc/comment `register_project/7` → `/8` arity references.
- `skills/harness-driver/SKILL.md` — `dispatch-register_project` row updated to document `warm_paths`.
- Tests: `dispatch_test.exs`, `settings_live_test.exs`, `components_test.exs`, `project_registry_test.exs` — real assertions covering the dispatch arg, mixed-delimiter parsing, blank-trim, and set-and-clear edit round-trips. No test-hiding patterns.

## Findings

1. **CHANGELOG gap (fixed).** Task 309 landed a user-visible authoring surface but had no `CHANGELOG.md` entry, despite the nearby tasks 252/284/308 all carrying one and prior audits (`178d753`, `67a5c38`) establishing the convention of adding entries for landed tasks. Added an `### Added` entry under `[Unreleased]` describing the dispatch arg, the dashboard textarea/parser, the `preserved_project_attrs` cleanup, and the arity-reference doc updates.

No other issues: code matches surrounding conventions, `@spec`s present, doc/arity references consistently updated, descripex surface and driver skill kept in sync, tests meaningful. No dead code, no debug output, no naming drift.

## Discoveries filed

None — nothing surfaced that warrants a follow-up rmap task.

## Reviewer-rejection note

The one recent rejection supplied (task 208, run `run-1780839809032-86dd1f20`, coverage 79.47% below the 80% threshold) does **not** intersect this landed range (tasks 309/310), so there is no false-rejection signal to report for this audit.

## Fixes applied

- `CHANGELOG.md`: added the task-309 `### Added` entry (doc-only; no project checks required).
