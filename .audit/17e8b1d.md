sha: 17e8b1de84dc218c1f8504eaff1fa859230fafb6
short_sha: 17e8b1d
audited_at: 2026-06-14
auditor: post-merge audit agent (grok)
verdict: hygiene fixes applied (docs + changelog + one small UX guard in new form)
findings: 4
fixed: 4

# Audit: 17e8b1d (tasks 284 + 285)

**Range reviewed (landed, already merged):**
- 17e8b1d roadmap: task 285 -> done (shipped 306016c240dd)
- 306016c harness: agent delivery — task 285 Retire config/dev.local.exs: strip dev.exs, run-to-apply seeds.exs.example, mem-threshold env var, boot guard, docs
- 0d59515 roadmap: task 284 -> done (shipped 759f4a5ca139)
- 759f4a5 harness: agent delivery — task 284 Dashboard: concurrency_cap editor + project register/edit/unregister form (close the last config-surface holes)
- (marker in_progress commits ignored per contract)

**What was reviewed:**
- Full diffs + stats of the two implementation commits (11 files for 285, 4 files for 284).
- Post-land source for: config/ (dev.exs now minimal, config.exs comment), priv/repo/seeds.exs.example (the run-to-apply script + header), mix.exs (new aliases + run_seed_if_present), lib/harness/project_registry.ex (warn_shadowed + configured_project_entries + call site), lib/harness/dashboard/{components.ex,settings_live.ex} (new project_settings_card + handlers + view-model helpers), added tests, updated docs (CLAUDE.md, skills/harness-driver/SKILL.md, priv/playbooks/dispatch-single-task.md), .gitignore, README.md, CHANGELOG.md.
- Grep for active (non-historical) references to retired dev.local.exs / config project blocks across *.ex, *.md, *.toml, scripts.
- Project test for the "config/dev.exs no longer declares..." guard + the new dashboard LiveView/component tests.
- Cross-check against task ACs in roadmap (for context only; did not edit roadmap).
- No run of full precommit.full (dialyzer cold cost); ran targeted: `mix compile --warnings-as-errors`, the exact dashboard test files covering the new surfaces, `mix credo --strict`, all green post-fixes. (Prior landed commits already cleared reviewer + hooks.)

**Findings (hygiene only — no dead code, no debug IO, no convention violations in the agent-delivered code itself):**
1. CHANGELOG gap: the Unreleased section had a detailed entry for the 283 upsert primitive (which forward-referenced 284/285) but no bullets documenting what actually landed in 284 (the dashboard card + LiveView handlers) or 285 (the retirement, seed script, env var, boot guard, doc updates). Fixed forward.
2. Stale user-facing docs in README.md "Use harness from another repo" section: step 1 still told operators to edit `config/dev.exs` with a `config :harness, :projects` list and restart — the exact flow retired by 285. (The 285 AC had claimed "no change required to README" on the theory that docs were already correct, but the landed state left the instructions describing the old mechanism.) Fixed.
3. Stale comment in config/config.exs (the "Registered orchestration targets" block) still claimed "Dev self-registers the harness checkout via config/dev.exs". Minor but incorrect post-285. Fixed.
4. Small UX guard missing in the new 284 form (components.ex): the edit row for an existing project rendered an editable `<input type="text" name="name" value=...>`. Submitting a mutated name would have treated it as "create under the new name" (preserved_project_attrs looked up by the submitted name, producing empty map; no rename path exists). This is not a crash but an easy way to produce orphan registrations instead of in-place edits of the other fields. Fixed forward with `readonly` (layout-preserving, still submits the identity, prevents mutation; new-form row stays writable; all existing tests continued to pass because they assert on other fields + submit `name:` explicitly).

**Other notes:**
- No leftover debug, no obvious naming drift, no broken @spec/@doc on the new public + private surfaces (all the added project_* helpers, parse_*, state builders carry `@spec`; the card has `@doc` + `@spec`; LiveView handle_events are conventional).
- The seeds.exs.example header is explicit ("NOT READ AT BOOT", points to dashboard / IEx / `mix harness.seed`), the upsert calls are correct (kwlist form accepted by the public API), the self-harness path uses `../../` (correct from priv/repo/), the boot guard + warning text are present and match the retirement intent.
- SKILL.md / playbook / CLAUDE.md updates that landed in the range were already accurate to the new flows.
- The isolation_test.exs still references a throwaway dev.local in a temp-dir fixture; that was explicitly called out-of-scope in the 285 AC and the fixture is testing worktree isolation (not the old seed path), so left untouched.
- Recent reviewer rejection log for task 208 (coverage < threshold on a rescue-pass) is from a prior range and does not overlap 284/285; no false-rejection signal to note for this audit.
- No discoveries filed as rmap tasks (the one latent in the form was directly repaired in this hygiene pass; no uncovered edge or deferred debt rose to separate-task level during the range review).

**Fixes applied (all in this audit commit, minimal diff):**
- CHANGELOG.md: added two concise bullets under Unreleased (modeled on the 283 entry) documenting the dashboard surface and the retirement/seed/env/boot-guard changes.
- README.md: replaced the obsolete "edit config/dev.exs" registration instructions + code block with current guidance (dashboard for running node; copy/edit/run `priv/repo/seeds.exs.example` via `mix harness.seed` or `ecto.setup` for fresh DBs) while preserving the check_command explanation.
- config/config.exs: updated the targets comment to describe the post-retirement seed/dashboard reality and note that dev.exs is now minimal.
- lib/harness/dashboard/components.ex: added `readonly` to the name input inside existing-project edit rows of `project_settings_card/1` (the one-line guard that makes "edit" mean edit-in-place for the mutable fields).

Result after fixes: the range is clean for docs, CHANGELOG, and the delivered surfaces. The audit commit will be the marker for the next audit.

(Reviewer-quality note: the 284/285 deliveries were sound; the gaps were exactly the sort of post-land hygiene an audit is for — no functional re-work was needed.)
