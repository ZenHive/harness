# Post-merge audit: `b1f31b4^..2af446e`

Reviewed the landed changes for Tasks 379, 380, 382, and 392, the Oban 2.24 / Phoenix 1.8.13 / quackdb 0.5.20 bump, the BEAM liveness-probe doc pin, and the accompanying reviewer fixups plus roadmap settlement commits. I checked the changed implementation, tests, CHANGELOG coverage, operational documentation, adapter pin, naming, leftover debug output, and whitespace.

## Findings and fixes

1. **CHANGELOG was silent on this range.** Tasks 379, 380, 382, 392, the Oban.Cron rename, and the vendor pin of `harness_agent_adapter` never made Unreleased. Delivery commits exclude CHANGELOG by design, so the gap is expected unless an audit fills it. Added the missing Unreleased entries.

2. **Live docs still described the pre-range pin and plugin names.** CLAUDE.md, AGENTS.md, README.md, and `skills/harness-driver/SKILL.md` still said `Oban.Plugins.Cron` and that the adapter subsystem is a git dependency, after mix.exs switched to `Oban.Cron` and `path: "vendor/harness_agent_adapter"`. Also updated the matching comments in `test/support/identity_fake_adapter.ex` and `Harness.Run.Worker`'s moduledoc. Historical CHANGELOG rows that were true at write time were left alone.

3. **Task 392 vendored the extracted adapter package to land tree-kill.** That is a working pin, but it forks `ZenHive/harness_agent_adapter` (Task 397's contract is the git dependency). Not reversed here — filed Task 418 to publish `OSProcess.kill_tree/1` upstream and restore the git pin.

No leftover debug, dead code, or convention breaks in the four task deliveries. Reviewer follow-ups on 379 (ancestor check vs ls-remote parse), 380 (fetch keyed by project name), 382 (snapshot refresh on store reset), and 392 (bounded reap wait, grandchild tests, MemoryGuard host RAM probe restored) look sound.

The supplied recent rejection concerned Task 208, which is outside this landed range, so there is no false-rejection assessment for these commits.

## Cold check

`mix deps.get && mix check.dispatch` in this intentionally un-warmed tree exited 0. Compile, Credo, Doctor, and Sobelow all passed. Hex reported the four adjudicated hackney 1.25.0 advisories during `deps.get`; those are not reachable (tzdata autoupdate disabled, transitive-only) and are not a regression of this range.

## Filed

- Task 418 — Publish Task 392 tree-kill in harness_agent_adapter and restore the git dependency pin.
