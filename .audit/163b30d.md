# Audit report: 163b30d (range e47340c, 163b30d)

**Range audited:**  
- e47340c harness: reviewer fixes — task 294 Settled witness event + FileSink for orchestrator wakeup (run run-1781506367005-8b414e0c)  
- 163b30d roadmap: task 294 -> done (shipped e47340c6d108)

**Scope:** Post-merge hygiene pass over the landed commits. Reviewed full diffs, touched sources (`config/config.exs`, `lib/harness/{dispatch.ex,notification/{event.ex,file_sink.ex},run.ex}`, `skills/harness-driver/SKILL.md`, new tests under `test/harness/{dispatch,notification,run}/`), pre-existing `docs/orchestrator-push-sink-design.md` (cross-referenced by the impl), CHANGELOG.md, Event/CaptureSink/Notification modules for context, and the design doc. Confirmed no roadmap edits performed (per rules). Ran local inspection equivalent to targeted checks; full `mix precommit.full` considered for code-touching edits (see below).

**Findings (3 total, all hygiene):**  
- **CHANGELOG gap.** The [Unreleased] section had no entry documenting the addition of the `:settled` witness + `FileSink` (Task 294). Sibling features (e.g. Task 289 `dispatch-await_runs`, 292/293 dispatch/lander work) were listed; this push-wakeup complement was omitted.  
- **Stale design doc header.** `docs/orchestrator-push-sink-design.md` (Task 290 spike) still opened with "Status: **decision recorded, not yet built.** ... follow-up build (Task 294)". The implementation matched the spike decisions exactly (fire site, payload via shared `summarize_result/1`, JSONL envelope, no-op unconfigured, isolation, driver pattern).  
- **Minor stale moduledoc.** `lib/harness/notification/event.ex` intro listed firing sites but only credited `Harness.Run` for `:in_run_discernment`; the new `:settled` path from `settle/2` was absent from the prose (typedoc + outcome docs were updated in the landed change; summary clause was added).

**Other review notes (nothing to fix):**  
- No dead code, debug prints, leftover TODOs, or broken conventions in the delta.  
- New code follows project rules: `@spec` on all new `def`/`defp`, `@doc false` + comment for the now-public `Dispatch.summarize_result/1` (internal cross-module use by Run/tests), concise moduledocs, failure isolation in sinks (local rescue + :ok, matching `CommandSink` and `Notification.deliver`), Jason-safe maps, consistent branch naming ("harness/" <> run_id).  
- Tests are useful and targeted (Event summary for settled atoms/tuples; FileSink happy/no-op/malformed rescue paths with tmp-dir + app-env seam; Run settle witness firing + payload equality to `summarize_result`). Pre-existing `CaptureSink` reused correctly. No trivial assertions.  
- Rename (`summarize` -> `summarize_result`) + extraction was clean; call sites (await_result, Run notify, tests) updated; no duplication.  
- Config comment example and SKILL.md paragraph addition are accurate and placed with siblings.  
- The supplied recent reviewer-rejection example (task 208, run run-1780839809032-86dd1f20, coverage 79.47% < 80% threshold on `mix precommit.full`) does not overlap this range (294) and was not relevant here. The 294 reviewer run produced "reviewer fixes" per the commit subject; the landed diff + tests read as sound (no indication of a false rejection that would require note or revert).  
- Roadmap files updated only by the marker commit (expected); left untouched per operational rules.  
- No design drift from the 290 spike (payload shape, fire point after `persist_run_record` + `broadcast_settled`, uniform envelope, `FileSink` as read-only Sink impl).

**Fixes applied (own edits, 3 files):**  
- Added concise entry under first [Unreleased] ### Added block (immediately after the paired Task 289 await_runs bullet) describing the witness, sink, config, tests, docs, and relationship to pull await.  
- Updated design doc status header to "implemented (Task 294, landed e47340c)" and clarified the spike/implement split.  
- Extended the Event moduledoc firing-sites sentence to name the `settle/2` `:settled` emission from Run (kept the original prose style).  

These are forward hygiene only; no behavior change.

**Clean range otherwise.** The core delivery (shared summarize projection, witness emission at the right lifecycle point, append-only sink with correct no-op + isolation, test coverage, driver doc) was already in good shape. Finding count reflects only post-land polish surface.

**Discovery filing:** None filed. The issues above were direct, local hygiene fixes (changelog omission, doc header drift, one-sentence prose gap) with no orphaned code path, uncovered edge, or deferred architectural decision worth a separate durable `rmap` task. The pre-existing lack of a catch-all `Event.summary/1` clause (would FunctionClauseError on unknown type) is an old pattern, not introduced or expanded by this range; no new rmap warranted for an audit pass.

**Next audit marker:** This report + the hygiene commits become the stop point for the subsequent audit (subject will be `audit(163b30d): ...`).

**Machine summary (see `.harness/audit.json` for the exact artifact):** 3 findings, 3 fixed, clean delta plus the expected doc surface for a witness/sink addition. No re-verification or land actions taken.
