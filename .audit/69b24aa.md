# Audit — 69b24aa (range 47ceca6..69b24aa)

Post-merge hygiene pass over the task 400 self-host guard, the Herdr
operator-side adjudication and notification configuration, and their roadmap
writebacks.

## What I checked

- `Harness.Git.TargetSync` detects self-hosting from path/filesystem identity,
  returns a named skip, and leaves the origin land authoritative. The lander and
  durable-roadmap callers retain their existing witness/log behavior.
- The tests pin off-target ref sync, clean checked-out sync, dirty/non-fast-forward
  skips, drift counts, direct self-host detection (including a symlink), and
  caller-level self-host behavior.
- The Herdr documentation keeps the tool operator-side and the tracked
  `CommandSink` configuration points to the installed executable operator script.
- The roadmap render changes match task 361's rescope, task 400's completed
  outcome, and task 401's in-progress transition. The review follow-up correctly
  restored the lander moduledoc list numbering.
- No dead code, leftover debug output, inconsistent naming, or malformed generated
  roadmap output was found in the landed range.

## Findings and fixes

| # | Finding | Action |
|---|---|---|
| 1 | Task 400 had no `[Unreleased]` CHANGELOG entry | Added a Fixed entry describing the self-host skip and preserved non-self-host behavior |
| 2 | `config/config.exs` claimed a missing Herdr script silently no-ops, but `CommandSink` logs rescued execution errors | Corrected the comment to match runtime behavior |
| 3 | The cold check reports that ExSlop is registered but its checks are discarded by the explicit Credo enabled list | Filed task 402; no unrelated `.credo.exs` change was folded into this audit |

## Reviewer-rejection cross-reference

The supplied rejection concerns task 208, which is not in this landed range. No
false-rejection note applies. Task 400's landed implementation is reviewer-approved
and matches its acceptance criteria.

## Discoveries filed

- **Task 402 — Activate the registered ExSlop Credo checks instead of discarding
  them from the explicit enabled list.** This is the separate Credo configuration
  concern already excluded from task 401.

## Verification

- Cold bootstrap: `mix deps.get`.
- Cold witness: `mix check.dispatch` passed. Full output:
  `/var/folders/ht/838bqmdn3c1g6f336kw3vc440000gn/T/harness-audit-69b24aa.XXXXXX.log.nprBfEdNZh`.
- Focused landed-path tests:
  `mix test.json --quiet test/harness/git_test.exs test/harness/lander_test.exs test/harness/roadmap/durable_test.exs`
  — 62 tests passed, 0 failures.
