# Audit — 2e6dd79

Range audited (already merged on `development`):

- `3d67c9c` harness: agent delivery — task 310 dispatch-await attach-to-in-flight-run wedges the MCP transport
- `d1596df` harness: reviewer fixes — task 310 (same)
- `2e6dd79` roadmap: task 310 -> done (shipped d1596df90cd2)

## What landed (Task 310)

Fix for `dispatch-await` attach-to-in-flight-run wedging the MCP transport:

- **`lib/harness/dashboard/mcp_plug.ex`** (new) — `Harness.Dashboard.MCPPlug` fronts the
  `/harness/mcp` forward. Session-creation POSTs still delegate to anubis's
  `StreamableHTTP.Plug`; session-bearing initialized tool requests
  (`ping`/`tools/list`/`tools/call`) dispatch directly to `MCPServer.handle_request/2`
  in the Plug request process, bypassing anubis's per-session single-flight queue so a
  long `dispatch-await` no longer starves read-only tool calls and honors `timeout_ms`.
- **`lib/harness/dashboard/router.ex`** — `/harness/mcp` forward repointed from the raw
  anubis plug to `MCPPlug`.
- **`test/harness/dashboard/mcp_server_test.exs`** — helpers repointed to `MCPPlug`; new
  Task-310 regression: a faked in-flight run, await with short `timeout_ms`, asserts the
  structured `:timed_out` summary returns while concurrent `dispatch-status` /
  `supervisor-list_runs` on the same session respond promptly.

## Review

Code is clean and consistent with project conventions: `@moduledoc` present, `@spec` on
every `defp`, tagged-tuple `with` boundary, no leftover debug output, no dead code. The
`else`-branch parse-error degradation (including the `{:more, …}` over-length body case)
is reasonable. Naming matches the surrounding dashboard modules. The diff is
appropriately minimal — a thin routing shim plus one targeted regression test.

## Findings & fixes

1. **CHANGELOG gap (fixed).** No entry existed for Task 310 — a notable behavioral fix to
   the MCP transport surface. Matches the same gap the prior audit (`7f89c62`) closed for
   Task 309. Added an `### Fixed` entry under `[Unreleased]` describing the wedge, the new
   `MCPPlug`, and the regression coverage. Doc-only edit; no code checks required.

No other hygiene issues found.

## Reviewer-rejection note

Task 310 does not appear in the recent reviewer-rejection list for this project (the listed
rejection was Task 208, unrelated). No false-rejection signal to record.

## Discoveries

None warranting an rmap task.
