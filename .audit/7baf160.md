# Audit 7baf160 (Tasks 247 / 257 + Text/Artifact extract + registry overlay)

**Range audited (already landed on development):**
- 63f3e30 (prior audit) → 7baf160 (8 commits): roadmap bookkeeping (247/257), agent delivery for 247 (reviewer selection soft-hint), refactor extract+centralize (Text/Artifact, LanderWorker.new_for_project, AgentAdapter.check_permission_mode, OSProcess.sigkill; KPI in-memory re-sort; task 258 filed), CLAUDE.md UI dispatch rule retirement, fix for 257 (ProjectRegistry effective-project overlay at single boundary).

**Delivery commits reviewed:**
- 9d40a31 — Task 247 harness delivery: reviewer selection deprioritize (AgentRegistry.available? soft only)
- af37372 — refactor(simplify) move call sites
- 60842ff — extract Harness.Text + Harness.Artifact + centralize enqueue/permission
- 0785d99 — docs(CLAUDE): retire blanket hand-build UI rule
- d21f8ee — Task 257 fix(registry): ProjectRegistry.lookup/list return landing-overlaid effective project
- (roadmap markers 7baf160, e976f4e, 56f8299 touch only roadmap/* and are excluded from edit scope per operational rules)

## Scope of review
- Non-roadmap diff across the range (`lib/harness/{run.ex,project_registry.ex,lander/worker.ex,run_diff.ex,run/recovery.ex,run/review.ex,agent_adapter.ex,agent_adapter/{codex,os_process,pi}.ex,audit.ex,cron/orchestrator.ex,dashboard/kpi_live.ex,lander.ex,lander/resilience.ex,text.ex,artifact.ex}`, corresponding tests, CLAUDE.md, CHANGELOG.md).
- Hygiene signals: dead code after extract/move, missing/stale @moduledoc/@doc/@spec (per Elixir convention: @spec on every def + defp), debug IO, bare TODOs, CHANGELOG gaps, inconsistent naming, project conventions (mantra: judgment in agents/verdicts, mechanical substrate only), reviewer false-rejection note.
- Centralization invariants: Text/Artifact used at all prior call sites; Landing.Settings.overlay applied only at ProjectRegistry read boundary (guarded by test grep); available? used only for soft prioritize order, never hard filter on reviewer slate.
- No edits to roadmap/tasks.toml or ROADMAP.md (per rules).

## Findings

### Actionable hygiene (fixed)
1. **CHANGELOG gaps for the two agent deliveries in range.** Task 247 (reviewer selection: deprioritize not eliminate cross-family reviewer on soft unavailable hint) and Task 257 (single-boundary effective project from registry) had no `[Unreleased]` entries. Added two style-matched bullets under `### Fixed` (most recent first) describing the behavior change, the mantra alignment, and the regression coverage / guard test. No other surface docs required updates (new modules have concise moduledocs + @spec + doctests; moved helpers have @doc/@spec at their new homes; CLAUDE.md change was already the correct process note).

### Notes (no action)
- **Code quality and conventions:** Landed changes are clean and minimal. New `Harness.Text` and `Harness.Artifact` are small, pure, fully specified (`@spec` on publics; no privates), with happy-path doctests only. All moved call sites (capping in run_diff/run, artifact reads in Review/Recovery, new_for_project, check_permission_mode, sigkill) are direct and correct. `ProjectRegistry` and `Lander.Worker` and `Run` trust the overlaid effective project as documented. `prioritize_reviewers/2` + `availability_rank/1` implement soft deprioritization exactly as the task and its tests require. No debug prints, no bare `TODO`, no judgment arithmetic re-introduced.
- **@spec coverage:** Primary heads carry `@spec`; multi-clause public fns and many defp follow the established project style (one @spec per arity for the API surface). The two new modules and the central helpers added in the refactor are fully annotated. No new violations introduced.
- **Sobelow inline skips:** Touched files (artifact.ex read/2, agent_adapter.ex read_rule_file) carry justified `# sobelow_skip ["Traversal.FileModule"]` with comments (harness-generated roots or harness-written rule files, never untrusted input). This pattern is pre-existing and widespread (~38 sites); the project convention is `.sobelow-skips` (via `--mark-skip-all`) rather than comments. No `.sobelow-skips` exists at root. Left as-is: fixing the whole tree is out of scope for a minimal range audit and would not be a "smallest correct change."
- **Duplicate UTF-8 repair helpers elsewhere:** `dashboard/{ops_feed,chat_live}.ex` have their own `trim_*_to_valid_utf8` for transcript caps. These predate the extract (which targeted prompt/diff paths in Run/RunDiff); not introduced here, not dead, left alone.
- **Roadmap and marker commits:** Pure status flips and the "task 258 filed" side-effect of the refactor commit are bookkeeping. Per operational rules we leave `roadmap/*` and do not touch status.
- **CLAUDE.md change (0785d99):** Correctly retires the old blanket "hand-build all UI" rule in favor of "incremental against a design system/spec is normal dispatch." Matches current guidance; no further edit needed.
- **False-rejection note (Task 208):** The recorded rejection (coverage 79.47% on a rescue-pass missing the verdict artifact) is for an older commit and unrelated run. None of 247/257 or the refactor touch coverage thresholds or the precommit gate. No action; noted for the feedback loop only.
- **Tests:** Useful regression coverage added for the critical paths (unavailable reviewer kept in slate for both auto and explicit pins; registry overlay effective value + guard test; new Text/Artifact behaviors; landing trigger + worker tests updated for the central new_for_project). No trivial asserts.

## Fixes applied
- `CHANGELOG.md` — two new bullets under `### Fixed` documenting Tasks 247 and 257 (the substantive agent deliveries whose effects reached `development` in this range).

## Outcome
The landed range is otherwise clean. The refactor centralization (Text/Artifact, permission-mode, sigkill, new_for_project, single registry overlay) is a net simplification with no behavior change and strong guard tests. The 247 fix prevents a soft hint from acting as a hard gate that would discard completed work — exactly the judgment/mechanical split. Deliveries are minimal, convention-faithful, and well-tested. No reverts, no blocks, no scope expansion.

**Findings:** 1 (CHANGELOG gap)  
**Fixed:** 1
