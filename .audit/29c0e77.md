# Audit report — 29c0e77

**Range audited:** `237086c..29c0e77` (9 commits since `audit(237086c)`)
**Date:** 2026-06-17

## Scope

Phase 22 "live-run-legibility" bundle landings plus a Dialyzer follow-up fix:

- `c01e5ac` roadmap: add phase 22 bundle (tasks 311–314) + milestone v0_15
- `05732cd` Task 312 — live run header: stage stepper + active-agent indicator + live token counter
- `91ff518` Task 313 — run timing: Status/gen_statem timestamps → live elapsed + per-stage durations
- `aa47fcc` fix(run): drop unreachable `maybe_capture_structured_failure` catch-all
- roadmap status commits (`ed51c75`, `75a39f2`, `04a9e04`, `29c0e77`, `8c5b7f3`)

Code touched: `lib/harness/dashboard/{components,live,tokens}.ex`, `lib/harness/run.ex`,
`lib/harness/run/{status,log_record}.ex`, plus matching tests.

## What I reviewed

- **Task 313 timing surface.** `started_at` + `state_entered_at` added to `Run.data()`,
  `Status`, and `LogRecord`; `stamp_state_entry/2` stamps every `:enter` callback and the
  crash-settle path. `from_log_record/1` was widened to a `%{__struct__: LogRecord}` map
  pattern with `Map.get` field access so legacy/replayed records without the new fields
  return `nil` / `%{}` instead of crashing — verified the tolerance is real, not asserted.
  The live `Elapsed` clock is driven by the existing 5s `:meta_tick` (`:now` re-assigned in
  both `mount` and the `:meta_tick` handler) — no new timer infra, matching the AC.
- **Task 312 dashboard header.** `stage_stepper` renders the `Status.state` enum as a
  mechanical pipeline; active-agent dot + stage-following token kind are pure renders of
  the state fact. Mantra-clean — no threshold/verdict derived from the new facts.
- **`aa47fcc` fix.** Confirmed sound: `data.adapter` is always a module atom, so the
  guarded `maybe_capture_structured_failure/1` clause (`when is_atom(adapter)`) is total
  over `data()`, and the dropped `_data` catch-all was genuinely dead (the only caller at
  `run.ex:1718` passes `data`). Dialyzer's `pattern_match_cov` flag was correct.

## Findings & fixes

| # | Severity | Finding | Fix |
|---|----------|---------|-----|
| 1 | minor | CHANGELOG `[Unreleased] → Added` had a Task 311 entry (added by the prior audit) but **no entry for Task 312** (live run header). | Added a Task 312 entry. |
| 2 | minor | Same gap for **Task 313** (run timing facts + the `aa47fcc` Dialyzer follow-up). | Added a Task 313 entry, noting the follow-up fix. |

No code defects found — the lib changes were already gated green by the cross-family
reviewer; the only hygiene gap was the missing changelog coverage. CHANGELOG edits are
doc-only, so no project-check re-run was warranted.

## Reviewer false-rejection note

The only rejection in the feedback loop (task 208, run `run-1780839809032-86dd1f20`) is
outside this commit range and unrelated to the audited tasks 312/313 — no false-rejection
assessment applies here.

## Discoveries filed

None — no follow-up tech debt or orphaned paths surfaced in this range.
