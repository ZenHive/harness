# Post-merge audit: 03a7a8f

Integrated revision: `03a7a8f5b1690a079e3609b9a65f2816140b0c68`.
Range: `95a1d048ccf25e1fb529192bf96b48071cf5a85a..03a7a8f5b1690a079e3609b9a65f2816140b0c68`.
Date: 2026-09-21. Auditor: Codex / gpt-6-astra, dispatched post-merge session.

**QA failed. Six findings; two fixed here; four repair tasks filed.** Landing remains settled. This audit did not restart, deploy, revert, or use an operator database/server.

## Review and findings

Reviewed the range's runtime and documentation changes: QA queue/evidence/dashboard and explicit auditor selection; rollout tooling and hook inventory; graceful shutdown/admission and fixture cleanup; shared document stores, response schemas and cron helpers; registry/model-block isolation; portable AGENTS generation; Insights publication repair; and Task 452's Inbox coverage projection. Checked accompanying tests, README, changelog coverage, workflow/driver docs, and retained verification artifacts. No leftover debug instrumentation was found in the reviewed runtime additions. README documents the Inbox behavior; existing changelog entries cover the other major operator changes. CHANGELOG was not modified.

1. **Fixed: Task 452 reintroduced a clone.** The configured zero-budget ExDNA check found the identical task-result unwrapping in `Inbox` and `RoadmapLive` (41 AST nodes). Extracted `Harness.Dashboard.LoadError.from_stream/1`; both consumers preserve existing error and timeout behavior. Added a real concurrent-source/timeout regression and abnormal-result checks. The clone budget remains zero.
2. **Fixed: stale supervision documentation.** The parallel-dogfooding section still called `Harness.Run.Supervisor` a DynamicSupervisor. It now describes the admission fence and inner `Harness.Run.DynamicSupervisor`, matching the landed shutdown design.
3. **Tracked: inconsistent audit selection snapshot — Task 454.** `Audit.run_auditor/7` captures settings, `select_auditor/1` rereads them, and model selection then uses the earlier capture. A concurrent selection change can combine an adapter with a different snapshot's model or the standing fallback. This is a source-proven race, not an observed production incident. Runtime routing repair receives normal implementation and independent review.
4. **Tracked: integrated model-configuration failures — Task 455.** The detailed same-seed run produced 24 Cron tests and one Maintenance test failing through `Config.agent_model/1` into `Keyword.get/3`. The producer of the invalid namespace has not been established; no fallback or weakened assertion was added. Task 362's completed persisted-block isolation, Task 326's EPIPE investigation, and Task 432's dependency bootstrap concern different causes.
5. **Tracked: AgentDriver fixture availability — Task 456.** The diagnostic run additionally failed `AgentDriverTest` at line 72 because `CodexChannelEcho` was unavailable. The async test case precedes its helper module in the same file; compilation ordering is a hypothesis to prove. No production adapter defect is claimed.
6. **Tracked: one private security triage item — Task 457.** Additional scanner diagnostics require private assessment. Details and raw diagnostic output are confined to the repository's private draft advisory created by this audit. No exploit has been demonstrated. Public evidence intentionally excludes that diagnostic.

Tasks **454, 455, 456, 457** were created with `rmap new --from-stdin --tasks-path /data/postgresql/harness/worktrees/base/landing/run-1789981191274-f7570250/roadmap/tasks.toml`. Exact supplied catalog models are pinned: codex/gpt-6-astra, cursor/cursor-grok-4.6-high, grok/grok-4.6, codex/gpt-6-astra respectively. Generated roadmap artifacts are included solely to preserve these explicitly requested discoveries.

## Integrated QA before edits

Every configured check ran on the unmodified integrated revision before any source, documentation, or roadmap edits. The cold worktree lacked dependencies; `mix deps.get` succeeded without changing `mix.lock`. No credentials or prerequisites prevented completion of the configured checks.

| Command/check | Actual outcome |
| --- | --- |
| `mix precommit.full` (cold attempt) | Exit 1: AGENTS check passed, dependencies absent. |
| `mix deps.get` | Exit 0; locked dependencies fetched. |
| `mix precommit.full` (prepared attempt) | Exit 1, stopped at the test subprocess's exit 2. |
| `bash scripts/sync-agents-md.sh --check` (alias step) | Passed. |
| `mix harness.deps.check` (alias step) | Completed; no blocking result. |
| `mix format --check-formatted` (alias step) | Passed. |
| `mix compile --warnings-as-errors` (alias step) | Passed; dependency compiler warnings are retained in the log. |
| `mix credo --strict --ignore TagTODO,TagFIXME` (alias step) | Passed; 608 source files, no issues. |
| `mix doctor --raise` (alias step) | Passed; 315 modules, 100% moduledoc/doc/spec coverage. |
| `MIX_ENV=test mix test.json --quiet --cover --cover-threshold 80 --summary-only --exclude integration --exclude live_agent` (alias step) | Failed: seed 731642, 2,657 passed, 25 failed, 176 excluded, 0 skipped; 82.57% coverage, 80% floor met. |
| `mix sobelow --exit --skip` (independent remainder) | Exit 0; warned that the nonstandard router was not found. This is a coverage limitation, not proof that all router checks passed. |
| `mix ex_dna --max-clones 0` (independent remainder) | Exit 1: one exact Inbox/Roadmap clone; repaired here. |
| `mix reach.check --arch --smells` (independent remainder) | Exit 0: architecture OK; 18 advisory smell reports retained. These do not make the configured non-strict check fail. |
| `mix dialyzer.json --quiet` (independent remainder) | Exit 0: zero warnings, Elixir 1.20.4 / OTP 29 / Dialyzer 6.0.3. |

The alias explicitly excludes integration/live-agent tests; those boundaries are not represented as passed. No Rust component check is configured for this Elixir repository. Fleet Clippy mappings were reviewed as configuration, not executed against sibling repositories.

## Failure investigation

`MIX_ENV=test mix test.json --failed --no-retry --output /tmp/audit-03a7a8f-failed.json` passed all 25 selected tests (seed 193151). This did not erase the integrated failure.

**Reason for one broader diagnostic run:** 25 failures that disappear in isolation indicate an unresolved cross-module interaction, while the configured `--summary-only` output discards their names/stacks. Before editing, ran:

```sh
MIX_ENV=test mix test.json --quiet --cover --cover-threshold 80 --no-retry --seed 731642 --exclude integration --exclude live_agent --output /tmp/audit-03a7a8f-repro.json
```

Exit 2: 2,656 passed, 26 failed, 176 excluded; 82.54% coverage. Detailed failures: 24 `RoadmapPollerTest`, one `Maintenance.ControlTest`, and one `AgentDriverTest`. Full names and stacks are preserved in `03a7a8f/repro.json.gz`. The original run contains summary-only evidence, so its individual failures cannot be reconstructed from that output. No further full-suite rerun was made, and no fix for these failures is claimed.

An additional router-aware security scan after the hygiene edits exited nonzero. The router itself was unchanged. The exact command, diagnostic, limitations and assessment request are in the private draft advisory; Task 457 owns resolution. This reinforces the failed QA judgment without publishing sensitive details.

## Hygiene verification

- `mix format` on the four changed Elixir files, followed by `mix compile --warnings-as-errors`: passed.
- `mix format --check-formatted lib/harness/dashboard/load_error.ex lib/harness/dashboard/inbox.ex lib/harness/dashboard/roadmap_live.ex test/harness/dashboard/load_error_test.exs`: passed.
- `mix credo --strict --ignore TagTODO,TagFIXME --files-included lib/harness/dashboard/load_error.ex,lib/harness/dashboard/inbox.ex,lib/harness/dashboard/roadmap_live.ex,test/harness/dashboard/load_error_test.exs`: passed. The CLI reported scanning 610 files despite the intended file restriction; this was broader than intended.
- `MIX_ENV=test mix test.json test/harness/dashboard/load_error_test.exs test/harness/dashboard/inbox_test.exs test/harness/dashboard/roadmap_live_test.exs --no-retry --output /tmp/audit-03a7a8f-focused.json`: **60 passed, zero failures/exclusions/skips**.
- `mix ex_dna --max-clones 0`: passed, zero clones across 309 files.
- `bash scripts/test-sync-agents-md.sh`: passed portable rendering, upstream parity, idempotence, drift, missing-import, depth and HOME-isolation checks, before edits.
- All six compressed Task 326 artifacts match their recorded uncompressed SHA-256 values.
- `rmap new` validated and rendered each filing; `rmap validate --tasks-path roadmap/tasks.toml` passed for the final task set. `git diff --check` passed, and all committed evidence digests and required audit JSON fields were validated.

These checks verify the bounded hygiene changes; they are not a green full-QA or independent reviewer verdict for the integrated tree. Full QA was not rerun after the fixes.

## Reviewer-quality assessment

No false rejection established. Task 326's rejected investigation lacked completed probes and retained diagnostics; the later landed artifacts explicitly limit the conclusion to non-reproduction and retain raw failures. Their hashes match. Task 448's rejected implementations were subsequently changed by rescue/rollout work; sound later code does not invalidate the earlier rejections. Tasks 444 and 208's cited rejections are outside this delivered range and do not establish defects in these deliveries.

## Durable evidence

Logs and JSON are gzip-compressed under [`03a7a8f/`](03a7a8f/). [`manifest.json`](03a7a8f/manifest.json) records uncompressed SHA-256 digests and byte counts. `full.log.gz` preserves the complete prepared alias output; `suite.json.gz` preserves its parsed coverage/summary; `checks.jsonl.gz` records independent command exits; `repro.json.gz` preserves detailed failures; `focused.json.gz` and `fix-checks.jsonl.gz` record hygiene verification. The private scanner diagnostic is deliberately absent from git. Temporary paths in commands are provenance; the committed copies survive worktree cleanup.
