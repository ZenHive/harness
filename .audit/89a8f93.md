---
sha: 89a8f935a172c1117a7e1de44cbd5eb8f0107e30
short_sha: 89a8f93
audited_at: 2026-06-06
auditor_model: grok
verdict: 1 finding, 1 fixed forward
---

# Audit: 89a8f93 (range covering 89a8f93..74dd148 + follow-ups)

Post-merge hygiene pass over the landed range for Task 233 (cron-as-orchestrator-AI: mechanical upside-count gate + `Harness.Cron.Orchestrator` full-context planner), Task 236 (review-only salvage / `dispatch.rereview` on retained branch), Task 231/212/227 roadmap markers, the reviewer-fallback-counts migration version collision fix (a05764b), the driver-skill framing correction for Task 233 (752953e), and incidental roadmap bumps / .claude/settings.

## Reviewed

- **Landed commits** — 89a8f93 (task 231 done), 38d9c50 (212 in_progress), a760d61 (227 in_progress), 7bc5770 (231 in_progress), a05764b (migration rename for collision), 195f826 (236 done), 74dd148 (Task 236 agent delivery: `Dispatch.rereview/1`, `Run` review_only? path + early route, tests), 752953e (driver-skill doc correction), a41c762 (.claude/settings), f3b5648/bc63160/1357348 (236/233 markers), fdc6aee (Task 233 core: new `Cron.Orchestrator`, poller rewrite, `Roadmap.ready/1 :fields`, tests + CHANGELOG). Net delta in scope: `lib/harness/cron/{orchestrator,roadmap_poller}.ex`, `lib/harness/{dispatch,run,roadmap}.ex`, corresponding tests, one migration file rename + CHANGELOG, one-line driver skill fix.
- **Roadmap** — multiple commits touch `roadmap/tasks.toml` / `data.json` / `ROADMAP.md`. Left completely untouched per operational rules ("Never touch the roadmap").
- **Dead code / orphan paths** — grepped the poller/orchestrator surface and dispatch/run for retired names (`@default_agent` as poller default, blind fan-out paths, old `select_reviewer` etc.). Only historical comments in the new files (correctly labeled "retired") and pre-existing dashboard UI config for manual dispatch default (separate concern, still supported in `Config` + `SettingsLive`). No live code paths or duplicated dispatch logic remain. The N=0/1/≥2 gate in `dispatch_decision/2` and the plan artifact reader are the only paths.
- **Debug / instrumentation** — no `IO.inspect`, `dbg(`, stray `puts`, or unguarded `Logger.debug` in the new cron, dispatch, or run paths. Logging uses `Logger.info` for observable operator events (plan skips, autonomy disabled) and `debug` only for routine ingest skips — matches project pattern.
- **Bare `TODO` debt** — none introduced in the changed files. All temporary notes (if any) carry `TODO(Task N)`.
- **Elixir / project conventions** — new public functions carry `@spec` (`Orchestrator.plan/2`, `prompt/1`, `context/2`, `read/1`, `Dispatch.rereview/1`, `rereview_opts/3`, `Run` internal review_only routes, `Roadmap.ready/1` with fields). Moduledocs on the new `Orchestrator` and updated `Dispatch`/`Run` describe the mechanical vs judgment split. Test support (`RereviewCountingAdapter`) uses `@moduledoc false`. No `@tag :skip`.
- **Stale docs / framing** — the landed 752953e commit already corrected the driver-skill (`skills/harness-driver/SKILL.md`) prose from "blind fan-out" to the Task 233 gate + orchestrator model + `:fields` opt. Searches in `docs/` for old poller language turned up nothing. `agent-gate-workflow.md` and related were not touched by this range and did not require updates for these deliveries (the 228 audit already refreshed reviewer routing).
- **CHANGELOG coverage** — Task 233 has a detailed Added entry (upside-count gate, orchestrator artifact, no blind fan-out, `:fields`, retired default_agent). The migration collision fix has a clear Fixed entry. Task 236 (`rereview` / review-only salvage) had no entry despite adding a documented public surface (`Dispatch.rereview/1`, new run path, MCP tool) and being called out in the commit message and `Dispatch` moduledoc. The 228 "reviewer-stage mechanical fallbacks" entry is adjacent but does not name the 236 primitive.
- **Naming / symmetry** — `rereview` / `review_only?` / `review_only_agent_diff_size` are consistent with the existing `resume_failed` / `resume_*` family and the `review_*_count` witnesses from 228. The early `route_after_dispatch` clause for review_only is a clean one-clause guard.
- **Migration hygiene** — the fix commit (a05764b) renamed only the colliding `add_reviewer_fallback_counts...` from 20260605010000 → 20260605011000; the sibling `add_review_skills...` (10000) was left alone. Both files now coexist with unique versions. The migration module itself is a standard Ecto alter with no content issues.
- **Tests** — new orchestrator tests cover artifact read (happy, missing skip, malformed, missing dispatch list), prompt construction, and injectable plan via config. Poller tests cover N=0/1/≥2 decisions, undispatchable logging, plan skip logging, and error paths. Dispatch tests added rereview-specific adapter double and cases. No obvious gaps for the hygiene scope.
- **Checks (pre-fix)** — the tree at audit base was the result of prior hooks + the landed commits; `mix precommit.full` will be run after the hygiene edit.
- **Reviewer false-rejection context** — no reviewer rejections recorded for this project in the provided feedback. The range includes Task 231 (reviewer_flaked KPI attribution) and 232 (reviewer transcript persistence) which improve diagnosability of stuck reviewers; nothing in the landed work contradicted those.

## Found & fixed (1)

1. **CHANGELOG gap for Task 236.** Added a concise Unreleased Added bullet documenting the review-only salvage primitive, the `Dispatch.rereview/1` surface (and MCP counterpart), the `review_only?` early-route in `Run`, the distinction from `resume_failed`, and that judgment remains with the reviewer. Matches the style and level of detail of sibling entries (228 fallbacks, 229 recovery, 233 orchestrator).

## Reviewer false-rejection note

No reviewer rejections recorded for this project.

## Verdict

The range is sound. The Task 233 delivery correctly embodies the agent-gate mantra (mechanical count gate in the poller; full-context judgment and the plan artifact written by a cross-family orchestrator AI; harness only validates + enqueues). Task 236 adds a minimal, mechanical review-only entry point that re-uses the existing reviewer gate and 228 fallbacks seam without introducing new judgment in code. The migration collision fix was the correct mechanical resolution. The one hygiene item was the usual post-delivery omission (CHANGELOG for a new public surface + distinct primitive). Fixed forward with a single targeted edit; no behavior or doc drift elsewhere. `mix precommit.full` will be exercised to confirm the tree stays green.
