# Audit 9749540

**Range audited (already merged on development):**
- 9749540 review: tighten Task 288 gate — MCP language param test + Invocation doc
- a6b3d1c harness: agent delivery — task 288 Re-thread language-aware rule injection from %Project{} (Task 124 regressed when CheckStack was deleted) (run run-1781474881045-75fd48db)
- e0907c3 chore(deps): bump tidewave 0.5.6 -> 0.6.0 (dev)
- 440dac5 docs(skills): sync harness-workflow + harness-driver — 286 idempotency, :recovering self-heal, routing/model mgmt, cron manual-approval

**What was reviewed:**
- Per-commit diffs + cumulative stat (15 files, +242/-28 net in the agent delivery + review + sync; dep bump and prior audit marker excluded).
- Core changes: language added to `%Harness.Project{}` (struct default, type, moduledoc), `fetch_language` + wiring in ProjectRegistry (build_project, api docs), Dispatch.register_project/7 + api() schema param + description, Run (3 invocation builders now pass `language: data.project.language` for implementer/recovery/reviewer), RulesInjection.render_opts updated for explicit nil/:elixir vs non-elixir exclusion.
- Test additions: LanguageCaptureAdapter (fake adapter that captures language into committed txt files for git-show assertions in lifecycle_test); tests for implementer + reviewer + recovery paths threading the project language (including :typescript); extended rules_injection_test for claude/codex/cursor/prepend across :elixir explicit, nil default, :rust, :typescript (exclusion of elixir_conventions + verification_gates).
- Schema/test gate: dispatch_test.exs MCP tool schema now asserts `has_key? :language` and updated comment on defaults vs required (review commit 9749540).
- Doc updates in range: CLAUDE.md Project bullet (added language clause), the 440dac5 skill/workflow include sync (286/287/ routing details), Invocation @typedoc correction in review commit.
- Cross-checks: no references to deleted CheckStack machinery in active code (only historical in CHANGELOG/CLAUDE/AGENTS — expected); all new/edited public + private fns carry @spec; style matches surrounding (pattern-matched private helpers, tagged error returns where relevant, functional tests); no IO.inspect/dbg/leftover debug; no disabled checks or @tag :skip; tests added cover real behavior (threading + rule exclusion on multiple delivery channels) rather than trivial paths.
- Surfaces: Project doc, Invocation doc, Dispatch MCP, registry lookup/register, Run data/invocations, RulesInjection, fixtures, and the two top-level agent context docs (CLAUDE/AGENTS).

**Findings:**
- One doc inconsistency surfaced by the range: the multi-project federation bullet in AGENTS.md was not updated when the equivalent text in CLAUDE.md received the `language` clause during a6b3d1c. (Parallel architecture descriptions had drifted on a now-restored first-class field.)
- CHANGELOG gap: the task 288 delivery (regression repair for language-aware injection, plus the MCP exposure and review tightening) had no entry under Unreleased, unlike nearby dispatch work (286/287) and similar past audits that included changelog hygiene for delivery ranges.
- No other issues: no dead code, no stale rule references, no naming drift, no missing coverage for the new language threading (the LanguageCaptureAdapter + assertions + rules tests are direct and sufficient), no convention violations in the landed code, review commit was minimal and correct (tightened exactly the gate + doc), dep bump and doc sync were clean. The unrelated reviewer-quality note (task 208 false-reject on coverage) does not overlap this range.
- Discovery: none filed. The doc drift and changelog were hygiene items fixed in this pass; no new tech debt, orphaned paths, or deferred decisions worth a separate rmap task were uncovered.

**Fixed (own edits):**
- Synced the exact `language` clause into the Project carries sentence in AGENTS.md (single occurrence in the federation bullet) to eliminate the drift with CLAUDE.md.
- Added a concise Fixed entry under Unreleased for the language re-thread / Task 288 (records the regression repair, threading, tests, review gate, and doc surfaces; mirrors the style and granularity of recent entries like the 286/287 idempotency and pinned-model fixes).
- (No source .ex changes; only docs + changelog hygiene + this report.)

**Result:**
The range is clean. The core re-threading is correct, minimal, well-tested, and follows project conventions (specs on defp, useful tests, no judgment in harness, language flows from Project only). The reviewer's tightening of the Task 288 acceptance gate (MCP schema presence + Invocation doc) was precise. Only the cross-doc drift (AGENTS/CLAUDE) and changelog record were material hygiene items; both addressed forward as part of the post-merge audit contract.

No re-verification or blocking; fixes are additive records only. This commit (audit(9749540)) serves as the marker for the next audit sweep.

(Generated by post-merge audit agent in worktree run-1781476766996-f44e7e39.)
