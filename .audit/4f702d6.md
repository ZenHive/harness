# Audit — range `23646d3..4f702d6`

**Date:** 2026-06-17
**Auditor:** post-merge audit agent (harness)
**Commits reviewed (3):**

- `4f702d6` harness: agent delivery — task 311 Transcript legibility — kill the content-less 'OTHER' wall
- `435a6bd` deps: update descripex 0.11.0 + phoenix_live_view 1.2.2; note exograph pin
- `b3b3fa9` harness: fix MCPPlug direct_post returning id:null parse error after Plug.Parsers drains the body

## What I reviewed

- **`4f702d6` (Task 311).** `lib/harness/dashboard/components.ex` transcript reducer + eyebrow rendering, and the `Cursor` parser tool-call resolution. Verified: every new `defp` carries a `@spec` (project mandate incl. defp); the new `:system_group` accumulator + `finalize_open/1` path is internally consistent (count-1-with-raw degrades back to a `:unknown` details block, otherwise emits a deduped `:system` row); the `eyebrow_summary/1` clauses cover the grouped shape and the raw `_type` shape; `tool_call_id/2` and `tool_call_entry/1` correctly prevent metadata siblings (`hookAdditionalContexts`, `toolCallId`) from shadowing the real `*ToolCall` payload. Component + parser tests exercise the coalesced row, the cursor round-trip with tool details intact, metadata-sibling resilience, and malformed-payload safety.
- **`b3b3fa9` (MCPPlug fix).** `direct_post/2` now routes through `fetch_message/1`, trusting parsed `conn.body_params` and falling back to `read_body/2` only without an upstream parser. Regression test mirrors the endpoint's `Plug.Parsers` step. Sound and well-documented.
- **`435a6bd` (deps).** descripex 0.10→0.11, phoenix_live_view 1.2.1→1.2.2, floki 0.38.3→0.38.4; exograph kept on the GitHub dep with a `TODO:`-prefixed pin rationale (correct per the project's TODO-prefix convention). Routine bump, no behavior change in repo code.

## What I found

- **CHANGELOG gaps (2).** The `[Unreleased]` section recorded neither Task 311 (a substantial transcript-legibility feature) nor the `b3b3fa9` MCPPlug `id:null` regression fix (a follow-up to the Task 310 entry that *was* present). The deps bump (`435a6bd`) was correctly *not* tracked — this CHANGELOG does not log routine dependency bumps as standalone entries, consistent with prior history.

Nothing else flagged: no dead code (the defensive `eyebrow_summary(%{"_type" => _type})` clause is reasonable forward-compat for parsers that carry `_type`, not dead), no leftover debug output, no stale docs, naming and `@spec`/`@moduledoc` conventions intact, the `TODO:` exograph comment follows convention.

## What I fixed

- Added a CHANGELOG `### Added` entry for **Task 311** (fallback `:system` coalescing + Cursor tool-call hardening).
- Added a CHANGELOG `### Fixed` entry for the **MCPPlug `direct_post` `id:null`** regression fix.

Both are doc-only edits to `CHANGELOG.md`; no source touched, so no project checks were rerun.

## Discoveries filed

None. No follow-up tech debt or uncovered edge case surfaced in this range.

## Reviewer-rejection note

The provided reviewer-feedback context references task 208 (a coverage-threshold rejection) — that task is **not** in this landed range, so no false-rejection assessment applies here. The three commits in this range each landed with their own regression/feature tests.
