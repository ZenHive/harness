# Post-merge audit for 14757be (range including 4b36aad + 02beb85)

**Audit date:** 2026-06-16  
**Auditor:** post-merge audit agent (this run)  
**Landed range reviewed (already on development, never reverted):**  
- 14757be roadmap: task 301 -> done  
- 02beb85 harness: agent delivery — task 301 list_available_models ... distinguish the two diagnostic states  
- c71eaa7 / cd22b06 / d582c83 roadmap status markers (task 301 reassignment + 300 done)  
- 4b36aad harness: task 300 — consolidate @recoverable_code_reload_states to single source in Harness.Run  
- a0543ba roadmap(300) follow-up filing marker (from prior audit)

## Scope and method
- Inspected the two harness code delivery commits (and only those; roadmap files and status markers are out of scope per operational rules).
- Reviewed diffs + full current sources for: `lib/harness/model_availability.ex`, `test/harness/model_availability_test.exs`, `lib/harness/run.ex`, `lib/harness/run/worker.ex`.
- Cross-checked consumers (`lib/harness/routing.ex`), tests, api() descriptions, and top-level docs (CHANGELOG.md, CLAUDE.md, docs/, AGENTS.md) for staleness or drift caused by these changes.
- Verified Elixir conventions (@spec on public + private, moduledoc, @doc false for internal, tagged tuples, no bare TODOs).
- Checked for dead code, debug left behind, naming inconsistency, missing back-compat, and CHANGELOG coverage.
- Ran local verification (compile, fast checks) on the post-edit tree.
- Reviewed the supplied "recent reviewer rejections" note (task 208 coverage failure on a prior range) — not applicable to this range.

## What was found
**CHANGELOG gap (actionable hygiene):**  
The delivery commits (02beb85, 4b36aad) touched only the implementation + unit tests. No entry was added for either Task 300 or 301 under Unreleased. Task 299's entry (in the same Unreleased Fixed section) explicitly called out the dupe states as a filed follow-up (Task 300), making the absence of the completion note noticeable. This matches the pattern fixed in the immediately prior post-merge audit ("changelog hygiene for Task 299...").

**Code / design hygiene — clean:**  
- **Task 300 consolidation:** `@recoverable_code_reload_states` is now defined once in `Harness.Run` (with comment noting the single-source intent and Task 300). Public reader `recoverable_code_reload_states/0` is present with `@doc false` + `@spec`. `Run.Worker` derives its compile-time module attribute via the function call and updated its comment to point at the shared source. The `normalize_crash_reason/2` guards and `recoverable_crash_reason?/1` continue to use the list (and retain the legacy broad `{:undef, _}` / `{:function_clause, _}` fallbacks for recoverability + retained-branch salvage, which is intentional per the 299 design and out of scope for the consolidation task). No duplicate list remains. No other call sites or copies found.
- **Task 301 diagnostic split:** `list_available_models/2` cleanly branches on `list_available` result. New private `list_available_models_unavailable/1` (with proper `@spec`) consults `catalog_universe/1` to emit the three states:
  - `{:ok, %{catalog_status: "available", catalog_unavailable: false, models: [...]}}`
  - `{:ok, %{catalog_status: "probed_none_selected", catalog_unavailable: true, models: [], universe_count: N}}`
  - `{:ok, %{catalog_status: "unavailable", catalog_unavailable: true, models: []}}`
- Backward compat explicitly preserved (the `catalog_unavailable` boolean + `models` key shape). `api()` description was updated in the same commit to document the three cases. New unit tests in a dedicated `describe "list_available_models/2"` block cover exactly the three cases from the task acceptance criteria (static selection, probe cache present but nothing selected, no universe at all for antigravity). Test helper `seed_probe_cache/2` mirrors the existing static seed helper.
- Consumer in `routing.ex:available_model_index/1` only destructures `%{models: models}` (plus error case) — unaffected and correct (empty models for the two "unavailable" statuses).
- No behavior change to dispatch gate (`list_available/1`, `catalog/1`, `available?/2` etc.). No changes to selection model or probe paths.
- No dead code, no debug IO, no commented-out branches, no inconsistent naming (status strings match the task spec language), no `@tag :skip` or disabled checks introduced.
- Docs outside CHANGELOG: no direct references in `docs/` to the internal constant or the new status strings that would have gone stale. CLAUDE.md and driver skill mention the surface at a high level (still accurate). AGENTS.md and other top-level files not impacted.

**Other notes:**  
- The supplied reviewer-rejection example (task 208, coverage 79.47% on a rescue-pass review) belongs to an earlier range and has no bearing on the 300/301 work reviewed here. The landed changes for 300/301 included tests and followed the "reviewer is the gate" contract.
- No other convention violations or broken links surfaced in the range.
- Finding nothing else actionable is a valid audit outcome for a tightly-scoped hygiene + consolidation + diagnostic-refinement pair.

## What was fixed
- Added two concise bullets under the first `### Fixed` section of `CHANGELOG.md` (immediately after the Task 299 entry that had noted the 300 follow-up):
  - Documented the single-source consolidation (Task 300).
  - Documented the three-state diagnostic split + test/api updates (Task 301).
- These are the only edits in this audit. The report itself (this file) is committed as the audit marker for the next range.

## Discoveries / follow-ups filed
None. The only gap found was the CHANGELOG documentation omission, which is ordinary post-merge hygiene and was closed inline by this audit (no separate tech debt, orphaned path, or deferred decision worth a new rmap task).

## Commands run (verification)
- Inspected commits/diffs via git (limited to non-roadmap paths).
- Read full sources + call sites for the touched modules.
- `mix format --check-formatted` and targeted compile verification (post-edit tree remains clean; no warnings introduced by the hygiene edit).
- `mix check.fast` (format + compile --warnings-as-errors + credo) judged sufficient for a docs-only hygiene fix on already-reviewed/landed code. (Full `precommit.full` available for PR/merge gates.)

**Outcome:** Range is clean apart from the documented changelog gap (now fixed). Work stands as delivered.

Marker for next audit: this file + the `audit(14757be): ...` commit.
