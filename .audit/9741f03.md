# Audit — 9741f03

**Range:** `01e3f8e..9741f03` (the unaudited span since `audit(2af446e)`; the dispatch
list named its tail, tasks 418/428/426/424 plus the hand-commits around them).
**Date:** 2026-09-13
**Cold check:** `mix deps.get && mix check.dispatch` — **green** (exit 0) in this
un-warmed worktree. Notably this is the first cold build against the restored
`harness_agent_adapter` git pin from task 418; it resolved and compiled.

## What I reviewed

Nine tasks landed in the range: 418, 428, 426, 424, 423, 419, 417, 385, 383. I read
each delivery diff plus the operator hand-commits (`a75dafb` Postgres socket config,
`dfee46e` unreachable `:push_rejected` clause, `208cbd8` audit/resolver implementer
gate), then swept for stale references to the surfaces those tasks changed, CHANGELOG
coverage, and repo-root debris.

## Findings

### 1. CHANGELOG stated the opposite of what shipped (fixed)

The `Unreleased / Changed` section still read *"`harness_agent_adapter` is a path pin at
`vendor/harness_agent_adapter` (Task 392)"*. Task 418 published the tree-kill upstream,
restored the git dependency, and deleted the vendor tree — making that line actively
false rather than merely stale, and it was the one entry task 418's own acceptance
criteria named (AC4) but the delivery could not write, since `CHANGELOG.md` is excluded
from delivery commits by design. Rewritten to describe the git pin.

### 2. Nine landed tasks, zero CHANGELOG entries (fixed)

Every task in the range shipped without a CHANGELOG row — the structural consequence of
the same delivery-commit exclusion, which makes filling them the audit's standing job
(as `audit(2af446e)` and `audit(a8cffbd)` also did). Added entries for:

- **Changed:** 428 (MemAvailable headroom gate; `mem_highwater_kb` removed, migration note)
- **Added:** 426 (`landing_policy: :pr`)
- **Fixed:** 424 (roadmap checkout sync before ingest/writeback), 419 (cron no longer
  re-dispatches landed tasks), 423 (cache generations survive non-build input changes),
  383/385/417 (coalesced — delivery staging drops `.harness/agent-rules.md`, the injected
  `AGENTS.md` header, and symlinks/build artifacts)

### 3. CLAUDE.md § Architecture described a two-policy landing surface (fixed)

Task 426 added `landing_policy: :pr` and updated `priv/includes/harness-workflow.md`,
`docs/agent-gate-workflow.md`, and `Harness.Project`'s typedoc — but not the
self-contained "Autonomous landing" bullet in CLAUDE.md § Architecture, which still read
as though `:auto` were the only landing policy. That bullet is deliberately self-contained
so it survives into `AGENTS.md`, i.e. it is exactly the text the cross-family reviewer
gates against. Extended it with the `:pr` path (branch push, `gh pr create`, deferred
writeback via `PRPoller`, no `TargetSync`) and mirrored the identical paragraph into
`AGENTS.md`.

**Caveat, stated plainly:** CLAUDE.md forbids hand-editing `AGENTS.md`, but its generator
(`~/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh`) does not exist on this node,
so mirroring by hand was the only way to avoid leaving the reviewer's rule surface stale.
Task 418's delivery `c047cd1` did the same thing for the same reason. That is finding 5.

### 4. Empty tracked file at the repo root (fixed)

`claude-workflow-test.txt` — zero bytes, tracked, added incidentally in `fbee21b`
(a deps bump). Removed.

## Filed

- **Task 430** — *Vendor the AGENTS.md generator into the repo so a dispatched agent can
  regenerate it instead of hand-editing a generated file.* The rule "never hand-edit
  AGENTS.md" is unsatisfiable from a run worktree because the generator lives on one
  operator laptop, so every worktree-side CLAUDE.md edit either hand-mirrors (418, this
  audit) or silently drifts — and `AGENTS.md` is the only rule surface the cross-family
  reviewer reads. Checked against siblings 252, 412, and the real near-miss 395 (whose
  word-ceiling gate presumes a renderable AGENTS.md but does not ship one); named in the
  filing.

## Considered and not filed

- `MemoryGuard.host_rss_kb/0` has no production caller after task 428. Kept deliberately:
  its `@doc` and the moduledoc both now state it is a diagnostic and explicitly *not* the
  admission-gate input. Documented dead-end, not an orphan.
- `mem_highwater_kb: 0` still appears in `test/harness/oban_dispatch_test.exs:923` — that
  is the assertion that the removed key is ignored, not a leftover.
- `bugs.md` at the repo root is an intentional defect log with a promote-to-rmap
  convention, maintained by earlier audits.

## Reviewer feedback loop

The one rejection surfaced for this project (task 208, run `run-1780839809032-86dd1f20`)
is not in this landed range, so there is nothing to assess as a false rejection. For the
record its stated reason was substantive and self-consistent: a missing verdict artifact
forced a rescue-pass review, and `mix precommit.full` failed on coverage at 79.47% against
the configured 80.0% threshold. That is a real red, not a judgment error.

## Not done

No code changed, so no re-run of the check stack beyond the cold witness above. Nothing in
the range was reverted, unmerged, or blocked.
