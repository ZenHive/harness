# Audit — range `dafee47..71f87b5` (3 commits)

Audited 2026-06-08. Marker commit: `71f87b5`.

## Range

| SHA | Subject | Kind |
|---|---|---|
| `71f87b5` | roadmap: task 213 -> done (shipped 864f8b513408) | roadmap-only |
| `864f8b5` | harness: reviewer-dispatchability keys on reviewer_eligible? alone, not implementer enabled? | code + test |
| `dafee47` | roadmap: file tasks 241/242 — dashboard /harness load-perf | roadmap-only |

## Reviewed

- **`864f8b5` (the substantive commit).** Bug fix in `lib/harness/run.ex`: `reviewer_dispatchable?/1` previously ANDed the implementer-level `AgentSettings.enabled?` gate in alongside `reviewer_eligible?`, making a "reviewer-only" agent (disabled implementer + eligible reviewer) unexpressable — a Claude pinned reviewer-only sank every run with `{:reviewer_unavailable, Claude}`. Fix keys reviewer-dispatchability on `reviewer_eligible?` alone, drops the now-unused `reviewer_enabled?/1`, promotes `reviewer_dispatchable?/1` to `@doc false def` for testing, and adds a regression test. Clean, minimal, well-commented; the dropped `reviewer_enabled?/1` has zero lingering references (`grep` confirmed). The mantra holds — this is mechanical gate plumbing, not judgment moved into code.
- **`71f87b5` / `dafee47` (roadmap writes).** `rmap validate` → `valid`. Task 213 carries a complete outcome layer (`status=done`, `shipped_in=864f8b513408…`, `verified=true`). Tasks 241/242 filed clean into the `dashboard-perf` bundle. No parse corruption, no missing outcome fields.

## Found

1. **CHANGELOG gap (minor).** The `864f8b5` reviewer-dispatchability fix — a run-sinking bug that affected every run on a reviewer-only-pinned project — had no `[Unreleased]` entry, although the repo maintains detailed per-fix `### Fixed` entries (two siblings present in the same section). This is the only finding.

## Fixed

1. Added a `### Fixed` entry to `CHANGELOG.md [Unreleased]` describing the reviewer-dispatchability fix, matching the established style of the surrounding entries (mechanism, impact, resolution, the both-flags-off note).

## Reviewer-rejection feedback

The supplied rejection (task 208, run `run-1780839809032-86dd1f20`) does **not** correspond to any commit in this landed range (range covers tasks 213 and 241/242 plus the `864f8b5` code fix). Not applicable here — no false-rejection assessment to make.

## Verdict

Range is sound. One hygiene fix applied (CHANGELOG entry). No code behavior touched; no checks required (doc-only edit). Never reverts, never blocks.
