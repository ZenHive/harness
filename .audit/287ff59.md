# Audit 287ff59 (Tasks 248 / 252 / 253 / 256 + migration guard)

**Range audited (already landed on development):**
- d6a7abb → 287ff59 (20 commits): roadmap bookkeeping for Tasks 247–253, agent deliveries for 248/252, fixes for 253/MCP coercion, Task 256 per-agent default model, boot migration guard, warm_paths persistence.

**Delivery commits reviewed:**
- 4706b1c — Task 248 AgentKPI `review_skills` repoint
- d804ed3 — MigrationGuard fail-fast + ProjectRegistry structural re-raise
- eb17b58 — Task 256 `Config.agent_model` + reviewer model threading
- a702a54 — Chat.Tools JSON-string keyword-param decode
- 840f689 — Task 252 per-project `warm_paths`
- c9a1b88 — Task 253 `dispatch-await` float `timeout_ms` coercion
- 42bf059 — harness-workflow AGENTS.md regen note (docs only)

## Scope of review

- Full non-roadmap diff (`33` files, `+719/-44`) across `AgentKPI`, `Worktree`, `Dispatch`, `Chat.Tools`, `Config`, `Run`, `Project`/`ProjectRegistry`, `MigrationGuard`, `ResultStore.Postgres`, `Audit`, dashboard settings, migration, and tests.
- CHANGELOG / CLAUDE.md / `priv/includes/harness-workflow.md` / `skills/harness-driver/SKILL.md` coverage for landed capabilities.
- Project conventions: `@spec` on new functions, no bare TODOs, no debug IO, mechanical-only harness code (agent-gate mantra), useful regression tests.
- Reviewer-quality feedback loop entry for Task 208 (unrelated to this range; noted below).

## Findings

### Actionable hygiene (fixed)

1. **CHANGELOG gaps (4 entries).** Task 256 was documented on landing; Tasks 248, 252, and 253, plus the migration-guard boot fix and MCP keyword-string decode, had no `[Unreleased]` bullets. Added style-matched entries under `### Added` (warm_paths) and `### Fixed` (review_skills rollup, MigrationGuard, float timeout + JSON-string filters).

2. **Stale harness-driver skill (2 spots).** `skills/harness-driver/SKILL.md` still described LogRecord quality as `review_ratings` only (dark since v0_13) and claimed worktrees have "no mechanical bootstrap" — contradicting `Worktree.warm/2` shipped in Task 252. Updated LogRecord coverage to `review_skills` + legacy fallback and rewrote the worktree-provisioning paragraph to describe default + per-project warming.

### Notes (no action)

- **Code quality:** Landed implementations are clean — `@spec` on all new public/private functions in the touched modules, no debug IO, no bare TODOs, no judgment-in-code regressions. Tests cover the real boundaries (review_skills precedence, warm_paths seeding, float timeout_ms, JSON-string filters, migration guard raise, agent_model precedence, structural PG error re-raise).
- **Task 256 docs:** `CLAUDE.md` model-precedence paragraph was updated on landing; CHANGELOG entry present. Driver skill still omits `Config.agent_model` precedence in the dispatch section — minor; the canonical detail lives in `CLAUDE.md` and `Run.reviewer_model/1`. Not expanded in this pass to avoid scope creep beyond the two clearly-wrong skill paragraphs.
- **`priv/includes/harness-workflow.md`:** New AGENTS.md regen/`--check` guidance (42bf059) is accurate. Propagation to `~/.claude/includes/` and the marketplace plugin is the operator's `scripts/sync-harness-skills.sh` step — outside this worktree commit.
- **Roadmap-only commits:** Status flips and task filing in the range are harness bookkeeping, not implementer edits — no hygiene issues.
- **False-rejection note (Task 208):** Recorded rejection cited sub-80% coverage on an older commit (`95ccb038`) during a rescue-pass with no verdict artifact. That task is outside this landed range; none of the Tasks 248/252/253/256 deliveries here relate to it. No revert warranted.

## Fixes applied

- `CHANGELOG.md` — four missing entries for Tasks 248/252/253 and migration guard.
- `skills/harness-driver/SKILL.md` — `review_skills` LogRecord coverage + accurate worktree warming paragraph.

## Outcome

The landed range is otherwise clean. Deliveries are minimal, well-tested, and mantra-faithful (counting in harness, judgment in reviewer artifacts). No reverts, no blocks.

**Findings:** 2 (changelog gap + stale driver skill)  
**Fixed:** 2