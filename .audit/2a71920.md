---
sha: 2a71920
short_sha: 2a71920
audited_at: 2026-08-25
auditor_model: codex
verdict: 3 findings, 3 fixed forward
---

# Audit: 2a71920

Post-merge hygiene review of the landed range from `a3b5c01` through `2a71920`, covering the Task 401 gate cleanup, chat-test synchronization and dependency cleanup, Tasks 358/403/404 plus reviewer fixes, roadmap transitions, and the refreshed project-health reports.

## Reviewed

- Read every code-bearing diff in the range and the final source for `Run.Worker`, `ResultStore`, `KPILive`, `MCPPlug`, `Project.Source.Github`, `SettingsStore`, `Store.EtsHeir`, and `ModelAvailability`, with their focused tests.
- Checked for dead code, duplicate helpers, debug output, bare temporary markers, malformed whitespace, missing specs/moduledocs, naming drift, stale task references, and Changelog coverage.
- Left `ROADMAP.md` and `roadmap/` untouched. The ExSlop activation warning observed by the cold check is already tracked by Task 402, filed in this range, so no duplicate discovery was created.
- The supplied reviewer-rejection feedback concerns Task 208, which is outside this landed range. No in-range false rejection was identified.

## Findings fixed

1. **Missing Changelog coverage.** Added Unreleased entries for the notable behavior delivered by Tasks 358, 401, 403, and 404: bounded/coalesced KPI refreshes, the restored full gate, loopback/source-URL hardening, and cached settings with out-of-band catalog revalidation. The test-only chat synchronization and removal of the unused direct `req` dependency are internal maintenance and do not need separate release notes.
2. **Stale CompareLive status.** `Harness.Dashboard.KPILive` still described Task 81 as pending although the roadmap marks it done. Removed the status qualifier so the deliberate-sibling documentation cannot drift again.
3. **Stale GitHub-source clone guarantee.** `Harness.Project.Source.Github` claimed any user-successful `git clone` URL would work, which became false when Task 403 introduced the explicit scheme allowlist. The moduledoc now names the accepted HTTP(S), SSH, `git://`, and scp-style address forms.

No behavioral defect, dead code, inconsistent naming, leftover debug output, or untracked follow-up was found in the landed implementation.

## Checks

- Cold bootstrap: `mix deps.get` succeeded in the intentionally unwarmed worktree.
- Cold witness: `mix check.dispatch` passed with exit status 0. Captured log: `/var/folders/ht/838bqmdn3c1g6f336kw3vc440000gn/T/harness-audit-2a71920.XXXXXX.log.LtLfASEofD`.
- `git diff --check` passed.
- No focused behavioral tests were added or rerun because the audit fixes change documentation only; the landed range already carries focused regression coverage for its behavior.
