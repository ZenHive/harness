# Post-merge audit: ef4c790

Integrated revision: `ef4c790651bbb98b36b0f80285ae782256dc110f`.
Range: `95a1d048ccf25e1fb529192bf96b48071cf5a85a..ef4c790651bbb98b36b0f80285ae782256dc110f`.
Date: 2026-09-21. Auditor: Codex / gpt-6-astra.

**QA failed. Nine findings, five fixed; four existing repair tasks recovered.**
Landing remains settled. No operator database/server, deployment, restart, revert,
or shared-history rewrite was used.

## Review and repairs

Reviewed the range's QA queue/dashboard, rollout and explicit audit routing;
shutdown/admission and fixture cleanup; shared stores and clone extraction;
AGENTS generation; Insights publication and revision-bound project evidence;
Inbox coverage; reclaim inspection; and durable landing writeback. Inspected
the related tests, driver/workflow documentation and verification artifacts.
No new runtime debug instrumentation was found. CHANGELOG was reviewed and left
unchanged under the operational rule; Tasks 386/451/453 lack dedicated entries,
but their module/driver/verification documentation records the shipped behavior.

The earlier overlapping audit commit `c508604a1afd007fa8695dab41e00f23773f19e5`
was retained on `audit/recovery/` after its push failed. Its fixes and Tasks
454–457 were absent from this integrated revision. Recovered its reviewed patch
for the first two findings and its original `.audit/03a7a8f*` evidence, preserving
provenance rather than presenting old QA results as this run's results.

1. **Fixed: dashboard clone.** ExDNA independently confirmed the identical
   stream-error unwrapping in Inbox and RoadmapLive. Recovered the shared
   `LoadError.from_stream/1` helper and concurrent-error/timeout regression.
   The zero-clone budget is unchanged.
2. **Fixed: operator documentation gaps.** Recovered the correction distinguishing
   the outer run supervisor from its inner DynamicSupervisor. Added driver
   documentation for reclaim's coverage fields and refusal of incomplete apply.
3. **Fixed: Task 386 API metadata.** The internal ResultStore writeback setter
   lacked the neighboring internal setters' `@doc false` convention; it is not
   an operator tool. Roadmap's options declaration omitted `:require_durable`.
   Corrected its local-fallback and independent-review documentation too.
4. **Fixed: stale Dispatch contract fingerprints.** Task 386 intentionally changed
   only `reland`'s API description and parameter description after the previous
   snapshot. Inspected `03a7a8f..ef4c790`'s Dispatch diff before updating the API,
   normalized manifest and MCP fingerprints. Function/arity, schema, implementation
   module exclusion, and full fingerprint assertions remain enforced.
5. **Fixed: stale landing-worker fixture.** The dashboard override test lacked
   the persisted run record and durable roadmap now required by writeback.
   It uses an isolated memory store and the existing real-Git roadmap fixture.
   Assertions verify the delivery SHA and origin roadmap's done/shipped/reviewer
   facts, not just the worker return value. Missing-target behavior stays tested.
6. **Tracked, Task 454:** audit adapter/model selection rereads settings after
   capturing its snapshot. Confirmed in source; runtime repair requires review.
7. **Tracked, Task 455:** prior integrated model-configuration failures remain
   supported by retained diagnostics. They did not reproduce in this seed;
   one passing order does not resolve the prior cross-module evidence.
8. **Tracked, Task 456:** the prior async AgentDriver fixture-availability failure
   remains supported by retained diagnostics; not reproduced in this seed.
9. **Tracked, Task 457:** existing private security triage remains open. Queried
   advisory states without publishing their details. The configured scanner's
   router coverage warning is not full security coverage. No new public security
   diagnostic or duplicate advisory was created.

Tasks **454, 455, 456, 457** were refiled from the retained audit with their same
identities using `rmap new --from-stdin --tasks-path` against this worktree's
`roadmap/tasks.toml`. The first submission rejected quoted IDs without mutation;
numeric IDs succeeded. Catalog pins remain codex/gpt-6-astra,
cursor/cursor-grok-4.6-high, grok/grok-4.6, and codex/gpt-6-astra. Generated
roadmap changes are included to preserve these explicitly requested discoveries.
No substantial runtime repair was folded into this hygiene commit.

## Integrated QA before edits

Every configured check completed at the original revision before edits.
The cold dependency failure was repaired with `mix deps.get`, without lockfile
changes. The prepared alias stopped at the failing suite; the remaining
analyzers were executed independently.

| Command/check | Outcome |
| --- | --- |
| `mix precommit.full`, cold | Exit 1: dependencies absent. |
| `mix deps.get` | Exit 0. |
| `mix precommit.full`, prepared | Exit 1 at suite subprocess exit 2. |
| AGENTS generation check, dependency guard, format, compile with warnings as errors | Passed before the suite; dependency warnings retained in full output. |
| `mix credo --strict --ignore TagTODO,TagFIXME` | Passed, 613 files, no issues. |
| `mix doctor --raise` | Passed, required documentation/spec coverage. |
| `MIX_ENV=test mix test.json --quiet --cover --cover-threshold 80 --summary-only --exclude integration --exclude live_agent` | Seed 478895: 2,710 passed, **5 failed**, 178 excluded, zero skipped. Coverage **82.78%**, 80% threshold met. |
| `mix sobelow --exit --skip` | Exit 0, with warning that the nonstandard router was not found. Coverage limitation retained. |
| `mix ex_dna --max-clones 0` | Exit 1: one Inbox/Roadmap clone, repaired here. |
| `mix reach.check --arch --smells` | Exit 0: architecture OK, 20 advisory findings. |
| `mix dialyzer.json --quiet` | Exit 0: zero warnings, Elixir 1.20.4 / OTP 29 / Dialyzer 6.0.3. |

The configured alias excludes integration/live-agent tests; those were not
represented as passed. No Rust checks are configured for this repository.
No unavailable prerequisite remained for the configured checks.

`MIX_ENV=test mix test.json --failed --no-retry --output /tmp/audit-ef4c790-failed.json`
reproduced **all five** failures in isolation (seed 998157): AgentEconomy metadata,
two Dispatch contract snapshots, landing-worker override, and Roadmap options.
Full names and stacks are retained. The integrated log also contains an
`erl_child_setup` EPIPE message, but none of these five failures establishes the
historical Task 326 AuditTest failure. No production spawn fix is claimed.

## Repair verification

- Changed-code formatting and `mix compile --warnings-as-errors`: passed.
- Changed-file format check, strict Credo and `mix ex_dna --max-clones 0`:
  passed; zero clones. Credo was run project-wide.
- `MIX_ENV=test mix test.json test/harness/dashboard/load_error_test.exs test/harness/dashboard/inbox_test.exs test/harness/dashboard/roadmap_live_test.exs test/harness/agent_economy_test.exs test/harness/dispatch/contract_test.exs test/harness/lander/worker_test.exs test/harness/roadmap_test.exs --no-retry --output /tmp/audit-ef4c790-focused.json`:
  **133 passed**, zero failures/exclusions/skips.
- `bash scripts/test-sync-agents-md.sh`: passed portable rendering, parity,
  idempotence, drift, missing imports, depth and HOME isolation.
- All six Task 326 compressed artifacts match their recorded uncompressed hashes.
- `rmap validate --tasks-path roadmap/tasks.toml` and `git diff --check`: passed.

The full suite was not repeated after repairs. These focused results do not
change the failed QA judgment for the original integrated revision and are not
an independent reviewer verdict.

## Reviewer-quality assessment

No false rejection established. The later Task 326 investigation retains bounded
non-reproduction evidence and raw failures, which the rejected attempt lacked.
Task 448's rescue and rollout changed the rejected implementation and explicitly
reflect subsequent workflow decisions; later sound work does not retroactively
invalidate the earlier rejection. The cited Task 444 and 208 rejected deliveries
are outside this delivered range. Task 386's five independently reproducible
integrated failures were missed by its focused review and repaired here.

## Durable evidence

Full logs and JSON are compressed under [`ef4c790/`](ef4c790/), with uncompressed
SHA-256 digests and byte counts in [`manifest.json`](ef4c790/manifest.json).
`prepared.log.gz` is the full alias output; `suite.json.gz` contains parsed
coverage/summary; `failed.json.gz` records failure diagnostics; analyzer logs and
`checks.jsonl.gz` record independent outcomes; `focused.json.gz` and
`fix-checks.jsonl.gz` record repairs. Temporary paths identify command provenance;
committed copies survive cleanup. The recovered older audit evidence remains
clearly attributed to revision 03a7a8f.
