---
sha: b17761f12b6a9d71794fd22ec0803d805d7ce575
short_sha: b17761f
audited_at: 2026-06-06
auditor_model: grok
verdict: 2 findings, 2 fixed forward
---

# Audit: b17761f (range 078708b..b17761f)

Post-merge hygiene pass over Task 214 — reconcile run landed-state against the
target branch (fix false "approved but not landed" dashboard signal).

## Reviewed

- **Landed commits** — `faa5f3d` (agent delivery), `ad2232e` (reviewer restore of
  `.cursor/rules/harness-operational.mdc` deleted out of scope), `1839557` (reviewer
  fixes that re-deleted the rule file), `b17761f` (roadmap settlement). Net code
  delta: `RunFeed.landed_sha/3`, `ResultStore.landed_sha/2` git reconciliation,
  `Live` wiring for projects-aware merge labels/filtering, two `run_feed_test` cases.
- **Debug / instrumentation** — no `IO.inspect`, `dbg(`, or stray `Logger.debug` in
  the new paths.
- **Bare `TODO` debt** — none introduced.
- **Convention hygiene** — new public/private surfaces carry `@spec`; judgment stays
  in git facts + roadmap witness (no fusion arithmetic). `@run_branch_prefix` duplicates
  the existing private `"harness/"` constants in `Worktree`/`RunDiff` — pre-existing
  pattern, not worth extracting in this pass.
- **Out-of-scope deletion** — implementer removed `.cursor/rules/harness-operational.mdc`;
  reviewer restored it (`ad2232e`); a follow-up reviewer commit (`1839557`) removed it
  again. Current tree has the file (restored state holds at tip). No further action.
- **CHANGELOG coverage** — Task 214 shipped with no Unreleased entry.
- **Test coverage** — branch-reachability and not-landed paths covered; the rebased-commit
  message grep fallback (`ResultStore.logged_run_sha/3`) was implemented but untested.
- **`mix precommit.full`** — green after fixes.

## Found & fixed (2)

1. **CHANGELOG gap** — added Unreleased entry for Task 214 (dashboard landed-state
   reconciliation via roadmap witness + git reachability/message grep).
2. **Untested rebased-land fallback** — added `run_feed_test` asserting landed when only
   the target-branch delivery commit (matching `(run <id>)` suffix) is reachable, while
   the retained `harness/<run-id>` branch tip is not.

## Reviewer false-rejection note

No reviewer rejections recorded for this project.

## Verdict

Range was sound; two minor hygiene gaps (CHANGELOG + one missing test for the grep
fallback). Fixed forward; `mix precommit.full` green.