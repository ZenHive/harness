# Audit 8af9f03 (range 994f247..8af9f03)

**Scope:** Post-merge hygiene pass over landed commits for Tasks 212 and 230 (plus their roadmap markers and the follow-up template fix d674ca9). Per harness audit rules: fix-forward only; never revert or block; roadmap/tasks.toml and ROADMAP.md are out of scope and untouched; report + fixes committed together as the audit marker.

## Range reviewed
- 8af9f03 — roadmap: task 230 -> done
- 3b457bf — harness: agent delivery — task 230 Cleanup: delete subsumed hard-fail paths once retry + recovery seam prove out
- (roadmap markers for 230/216/237/212)
- d674ca9 — fix(template): drop deleted ResultStore.File from in-repo harness config
- 994f247 — harness: agent delivery — task 212 Retire ResultStore.File + Chat.Store.File — Postgres-only, ephemeral when repo_enabled:false

Substantive changes: deletion of `lib/harness/result_store/file.ex` and `lib/harness/chat/store/file.ex`; addition of `Memory` backends; updates to facades, config/runtime, LegacyTermImport + mix task, dashboard inspector/settings, tests, term_codec, and the in-repo template.

## Findings
1. **Stale provenance default in ConfigInspector.** The derived "Result store / root" row in `derived_sections/0` still passed the pre-212 File-era default `Path.expand("~/.harness/results")` to the `field/4` helper. Runtime `store_part(:root)` correctly returns "memory:ephemeral" or "database:run_records", but the comparison used for `:default` vs `:config` provenance was anchored to a retired path. (Minor but real inconsistency left by the retirement.)

2. **CHANGELOG gap.** The `[Unreleased]` section (both Fixed and Added) contained no record of Task 212 (the backend retirement, Memory implementation, LegacyTermImport cutover, template follow-up) or Task 230 (the final hard-fail path removal now that the recovery seam is proven). Prior audits in this repo routinely surface such omissions as hygiene items.

No other issues rose to the level of a fix:
- Zero references to the deleted `ResultStore.File` / `Chat.Store.File` (or their paths) remain in `lib/` or `docs/` (only historical text in CHANGELOG.md, roadmap/*.toml, ROADMAP.md, and _build artifacts — expected).
- New `Memory` stores implement the full behaviours, use per-scope ETS, strip outputs on list (parity with Postgres), and carry `@spec` on every def/defp plus concise moduledocs.
- `LegacyTermImport` is correctly gated behind `repo_enabled` in `Harness.Application` (only self-hosts get the one-time import child); its functions follow the `@spec`-everywhere rule.
- `mix harness.import_results` is a thin, documented wrapper.
- Task 230 diff updated comments and removed the three short-circuit sites (`pollution || reason`, `pollution || :timed_out`, the DOWN-path fail); the new `PollutingCrashAdapter` test exercises the intended recovery path.
- Runtime config, test config, and inspector already reflected the Memory default for `!repo_enabled`.
- No debug IO, no disabled checks, no obvious naming drift, no expanded scope.

## Fixes applied (this audit)
- Updated `lib/harness/dashboard/config_inspector.ex`:
  - Wired `result_store_default(:root)` (new clause) into the Result store "root" field, mirroring the `settings_store_default(:root)` pattern.
  - Added the `:root` clause to `result_store_default/1` returning the same descriptive values the live `store_part/1` emits ("database:run_records" / "memory:ephemeral"). This makes provenance correct when the node is using its default backend choice.
- Added two compact entries under `## [Unreleased] / ### Added` in `CHANGELOG.md` documenting the Task 212 retirement + import story and the Task 230 hard-path cleanup. Kept tone and length consistent with neighboring bullets.

## Verification
- `mix compile --warnings-as-errors` — clean.
- `mix credo --strict` — clean (0 issues).
- `mix test.json --exclude integration` — 1525 passed, 0 failures (integration-tagged substrate/roadmap tests excluded by design; fixture noise in logs is pre-existing and unrelated to the audited changes).

## Assessment
The range is **clean** apart from the two low-surface hygiene items above. The Task 212 delivery was a textbook retirement (facade changes + new backend + gated legacy bridge + consumer template + test parity), and Task 230 correctly excised the now-subsumed hard-fail branches once the recovery seam (229) and retry (227) had bedded in. No judgment logic was added to harness; all meaning still lives in the reviewer AI artifacts.

No reviewer rejections were recorded for this project in the provided feedback, so nothing to cross-check for false-reject signals.

Marker commit will be: `audit(8af9f03): changelog + inspector default for 212/230 range (otherwise clean)`

(The `.harness/audit.json` summary is written but deliberately untracked.)