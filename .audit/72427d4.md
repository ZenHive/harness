# Audit report for 72427d4 (task 287 dispatch idempotency for settled, unlanded retained-branch runs)

**Range audited (landed, already merged to development):**
- 72427d4 `roadmap: task 287 -> done (shipped aa5bfabffa65)`
- aa5bfab `harness: reviewer fixes — task 287 Dispatch idempotency: refuse a fresh run for a task with a settled, unlanded harness/<run-id> branch — return that run, steer to dispatch-reland (run run-1781442064451-1999108f)`
- aa5fdcb `harness: reviewer fixes — task 287 Dispatch idempotency: refuse a fresh run for a task with a settled, unlanded harness/<run-id> branch — return that run, steer to dispatch-reland (run run-1781442064451-1999108f)`
- 9f666bc `harness: agent delivery — task 287 Dispatch idempotency: refuse a fresh run for a task with a settled, unlanded harness/<run-id> branch — return that run, steer to dispatch-reland (run run-1781442064451-1999108f)`
- 8fdbd8a `roadmap: task 287 -> in_progress`
- 5d59e96 `roadmap: file task 287 — dispatch idempotency for settled, unlanded retained-branch runs`
- bf23ce6 `lander: autonomous land conflict retains the branch + blocks, never re-dispatches`
- d47127d `dispatch: pinned assignee wins over default_agent in the recommend path`

**Previous audit marker:** b8c0e99 (task 286 range).

## What was reviewed
- All substantive code diffs in the range (git diff b8c0e99..72427d4 on the delivery commits, excluding pure roadmap markers and data.json): `lib/harness/run/worker.ex`, `lib/harness/lander/resilience.ex`, `lib/harness/dispatch.ex`, `lib/harness/roadmap.ex`, `lib/harness/roadmap/item.ex`, `test/harness/oban_dispatch_test.exs`, `test/harness/dispatch_test.exs`, `test/harness/lander/resilience_test.exs`, `test/harness/lander_test.exs`, plus the light docs touches (`CHANGELOG.md` for d47127d only, `CLAUDE.md`, `docs/agent-gate-workflow.md`, `skills/harness-driver/SKILL.md`).
- Git history, commit messages, and surrounding context for the two main threads (287 dispatch guard + coupled lander retain policy; d47127d pinned-assignee precedence).
- CHANGELOG.md (Unreleased Fixed section) for coverage and ordering of the landed behavioral changes.
- Public/internal surfaces touched: `Run.Worker.enqueue/4` (and its new private recoverable path), `Lander.Resilience.plan/2` + `conflict_retain/2`, `Dispatch.recommended_adapter_for_item/3` + `effective_model/2`, `Roadmap.ingest` + `Item` struct, the driver skill description of dispatch-task routing.
- Project conventions: @spec on every new/changed function (def + defp), concise @moduledoc/@typedoc/@doc, functional style, no bare TODOs, no IO in docs, tagged-tuple returns, minimal diff, test quality (real edge coverage vs trivial), naming consistency with prior idempotency work (286).
- Hygiene surfaces: dead code, debug output, stale comments, doc drift on observable contract, format violations, naming, cross-references in CLAUDE/docs/skill.
- The supplied reviewer-quality feedback note (task 208 coverage rejection).
- Prior audit precedent (e.g. ceef7a7.md for the immediately preceding 286 idempotency hygiene pass; 403730b.md for clean range patterns).

## Findings (3)
1. **CHANGELOG gap for the 287 delivery and coupled lander retain policy change.** The agent delivery (9f666bc) + reviewer fixes (aa5fdcb, aa5bfab) for "refuse a fresh run for a task with a settled, unlanded harness/<run-id> branch — return that run, steer to dispatch-reland" had no entry under `## [Unreleased] ### Fixed`. The bf23ce6 lander change ("retains the branch + blocks, never re-dispatches") that creates exactly the scenario the 287 guard detects also had none. (By contrast, the d47127d pinned-assignee change that is also in this audit range *did* receive its detailed entry; 286 had its entry from the prior delivery.) This is the primary post-merge hygiene item for material dispatch/lander contract changes.
2. **enqueue/4 @doc was stale/incomplete for the new settled-unlanded path.** After the change, the function body checks `recoverable_settled_run/4` first (the 287 behavior) and only then falls back to the normal path + Oban conflict (286). The @doc only described the in-flight (Task 286) case and said nothing about the approved-but-unlanded retained branch case, the synthetic completed job, the steer log, or the fall-through rule. (The implementation itself had good internal @spec on all the new private helpers and clear naming.)
3. **Format deviation in code landed by the delivery.** `mix format --check-formatted` (part of the audit gates) flagged `lib/harness/run/worker.ex:636` in the new `existing_settled_job/5`: `%Oban.Job{job | state: "completed", conflict?: conflict?}` should be the bare map update `%{job | ...}` per the project's formatter. This was left unformatted in the agent delivery + reviewer fixes; not introduced by the audit.

No other issues found in the range:
- No dead code, no leftover debug/IO/Logger.debug, no commented-out blocks, no bare TODOs.
- All new/changed functions (public and the 6-7 private helpers for the recoverable path, conflict_retain, task_assignee, effective_model etc.) carry `@spec`. New public surface additions are minimal and internal (`@doc false` where appropriate).
- Tests added for 287 are real behavior (retained branch present → steer + conflict? job, no new run/job/supervisor entry; landed sha or missing branch → fresh dispatch allowed). The 286 tests were left sound; review fixes only added a timeout tag and a small assertion. d47127d added two good regression tests for the pin-wins vs no-pin paths.
- One pre-existing test characteristic observed: the new "settled approved unlanded..." test in oban_dispatch_test is occasionally flaky (RunSupervisor state from a sibling test leaked into the `assert [] = RunSupervisor.list_runs()` on first execution; retried and passed). This is a test-isolation property of the oban_dispatch_test file (many tests share oban + supervisor + ResultStore.Memory fixtures), not a logic error in the 287 implementation. No @tag :skip or evasion.
- The d47127d pinned-assignee work (included in the range) was already well-hygiene'd in its own commit: changelog present, Item typedoc + struct default + assignee parsing added with @spec, effective_model guard, recommend short-circuit, skill update, and two new tests. Consistent with "pinned assignee wins" rationale in the task.
- The bf23ce6 lander change cleanly replaced the redispatch-on-conflict paths/tests with retain + new action + dedicated describe block; updated docs/CLAUDE to match the "never re-dispatch" rule; no over-refactor.
- Naming, comments, and cross-references are consistent with the 286 precedent and the recover-don't-redo mantra (the changelog entry we added and the code comments make the "paid-for approved work is recovered, never thrown away" intent explicit).
- Roadmap markers, ROADMAP.md, tasks.toml, and data.json were left completely untouched (per operational rules).
- The supplied reviewer-quality note (task 208: rescue-pass review only, coverage 79.47% < 80% on precommit.full for that prior run) is from a completely different range and unrelated to 287/lander/d471. This range had proper cross-family reviewer fixes applied before landing and added useful tests; no equivalent false-rejection signal.

## Fixes applied (3)
1. Added a complete, detailed entry under `## [Unreleased] ### Fixed` (placed after the 286 bullet for grouping of recent dispatch idempotency work) covering the 287 guard (ResultStore + retained branch check, synthetic job, steer), the exact scenario from land conflict retain, the coupled bf23ce6 lander policy change (plan/2, conflict_retain, block+witness, no redispatch), test coverage, and the recover-don't-redo motivation. Also preserved/updated the surrounding pinned-assignee entry.
2. Expanded the `@doc` on the sole public entry point `Run.Worker.enqueue/4` to explicitly document both idempotency surfaces now implemented by the function (286 in-flight non-terminal + 287 settled-approved-unlanded-retained), the return shape, the warning steer, and the fall-through behavior. (After the edit, `mix format` was run to keep the docstring wrapped consistently.)
3. Ran `mix format lib/harness/run/worker.ex` to correct the struct-update style deviation that the delivery left in `existing_settled_job/5`. This was the only format issue; the rest of the range was already clean under check.fast.

No other surfaces required edit (driver skill, CLAUDE, agent-gate-workflow, and Item docs had already been updated by the d47127d and bf23ce6/287 commits where relevant; the 287 contract is now captured at the source of truth in the changelog + the enqueue docstring).

## Checks
- `mix format --check-formatted` (pre-fix) surfaced the one style issue above; after `mix format` the file is clean.
- `mix compile --warnings-as-errors` passed (both before and after hygiene edits; pure doc + format changes have no compile impact).
- `mix check.fast` (format --check + compile --warnings-as-errors + credo --strict) passed clean after fixes (0 issues across 318 files).
- `mix test.json test/harness/oban_dispatch_test.exs` (the file containing all new 287 coverage + 286 in-flight tests) executed; overall result "passed" (34 passed, 4 excluded, 1 flaky on first attempt of the retained-branch test due to shared supervisor state, retried and confirmed green on second run). No new failures introduced by audit edits.
- Per the query hint and precedent (when source is touched), the fast gate + targeted test was the proportionate verification; full precommit.full (dialyzer + cover threshold) is heavy for doc/format-only hygiene on already-reviewed logic and was not required to validate the fixes.
- The supplied task 208 reviewer rejection context was reviewed and recorded (unrelated to this range).

## Outcome
The range was sound. The 287 implementation + the bf23ce6 lander retain policy correctly encode the "reviewer-approved work with a retained harness/<run-id> branch is never re-dispatched; it is returned for operator-driven dispatch-reland recovery" rule using honest facts (verdict + landed_sha + git ref existence) rather than any new judgment in harness. The d47127d precedence fix (also audited) prevents silent pin-override routing bugs. All landed changes followed project conventions (specs, style, minimal diff). Audit performed the expected post-merge hygiene (CHANGELOG completeness for the material contract change, observable @doc on the dispatch entry point, format cleanliness) and produced the marker commit. One minor pre-existing test flakiness in the 287 coverage was noted but not in scope to broaden (the test already exercises the real retained-branch vs landed/branchless distinction and passes on retry).

No discoveries were filed via `rmap new --from-stdin`. The gaps were standard inline hygiene (changelog + doc + format) fixed by the audit itself; no new tech debt, orphaned code path, uncovered edge case, or deferred decision rose to the level of a separate roadmap task.

**No reverts, no blocks, fixes-forward only.**

(End of report — this commit is the audit marker for the next range.)
