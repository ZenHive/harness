# Audit — 31cfd94 (range 0198b6b..31cfd94)

Post-merge hygiene pass over 31 landed commits. Most of the range is
roadmap filing / status writeback. Code-bearing deliveries:

| SHA | Subject | Kind |
|---|---|---|
| 31cfd94 | roadmap: task 363 -> done | roadmap |
| 5579a1a | task 363 mountable-consumer auth boundary | docs |
| 4e688cb | operator cockpit + restore cursor routing | roadmap + include |
| 3c4c8ec | drop cursor from delegation roster | docs (later restored) |
| 2b92893 | model-catalog overlay hole + drop agy allowlist | code (fix) |
| 5333001 | filing-rule default + grok 4.6 | docs |
| 7655dda | anubis_mcp 1.14.0 → 2.0.0 (task 384) | deps |
| 0c806c1 | hex deps.update within constraints | deps |
| b4d525e | task 378 durable roadmap writes ignore roadmap_path | code (fix) |
| 32e91e3 | retarget development references to main | docs |
| 7d95773 | codex standing default is gpt-5.6-sol | docs |
| 68dfb23 / 9398acc / 0198b6b | workflow routing / inline policy | docs |

The remaining commits are roadmap-only (375–386 filings, status
markers, AGENTS.md regen). Left those files alone except the new
discovery filing below.

## What I checked

- **Code hygiene** — task 378's `roadmap_target_branch` is typed, validated
  at registration, persisted via the existing ETF payload, and covered by
  split-repo / fallback tests. Git fetch refspecs were tightened to
  `refs/heads/<branch>`. Task 363 is docs-only and matches its ACs (no
  auth plug added). 2b92893 correctly merges the builtin catalog
  unconditionally and drops Antigravity's literal spawn allowlist.
- **Stale docs left by 2b92893** — Antigravity's moduledoc and
  `build_command/1` `@doc` still claimed catalog-id validation at spawn;
  `known_model_ids/0` had no remaining production caller.
- **CHANGELOG gaps** — 363, 378, 384, and the catalog-overlay fix had no
  `[Unreleased]` entries.
- **Stale routing copy** — 7d95773 updated `priv/includes/harness-workflow.md`
  (codex default `gpt-5.6-sol`, `gpt-5.5` retired) but `CLAUDE.md` still
  told drivers to pin `gpt-5.5`.
- **Operator-surface hole after 378** — `dispatch-register_project`, the
  settings form, and the seed example could not set
  `roadmap_target_branch`. A split-repo project still cannot be registered
  through the documented operator paths.
- No leftover `IO.inspect`/`dbg`/bare `TODO` in the code diffs. No
  false-rejection note: the supplied task-208 coverage reject is not in
  this range.

## Findings & fixes

| # | Finding | Severity | Action |
|---|---|---|---|
| 1 | Tasks 363 / 378 / 384 and the catalog-overlay fix missing from CHANGELOG | minor | Added `[Unreleased]` Security / Fixed / Changed entries |
| 2 | Antigravity still documented and exported the deleted catalog allowlist | minor | Updated moduledoc + `build_command/1` docs; removed `known_model_ids/0` and its test |
| 3 | `CLAUDE.md` still routed codex to retired `gpt-5.5` | minor | Rewrote the dispatch-routing paragraph; regenerated `AGENTS.md` |
| 4 | README mountable-consumer paragraph omitted the 363 auth warning | minor | Added the loopback / own-auth sentence |
| 5 | Seed example did not mention `roadmap_target_branch` | minor | Comment next to `target_branch` |
| 6 | Operator surfaces cannot set `roadmap_target_branch` | follow-up | Filed task **389** (not implemented here) |

## Reviewer-rejection cross-reference

The recent rejection in the feedback loop was **task 208**
(`run-1780839809032-86dd1f20`, `mix precommit.full` coverage 79.47% <
80.0%). It is **not** in this landed range — no false-rejection note
applies. Tasks 363 and 378 landed as reviewer-approved deliveries and
the committed diffs match their acceptance criteria.

## Discoveries filed

- **Task 389** — Expose `roadmap_target_branch` on
  `dispatch-register_project`, `/harness/settings`, the seed example, and
  the driver skill. Distinct from pending task 375 (validation of the
  pre-378 optional set; dashboard forms explicitly out of scope).

## Cold check

Ran `mix deps.get` then `mix check.dispatch` in this intentionally
un-warmed worktree (log
`/var/folders/ht/838bqmdn3c1g6f336kw3vc440000gn/T/harness-check-dispatch.XXXXXX.log.sD43z3NO8M`).
Format, compile `--warnings-as-errors`, Credo `--strict`, Doctor, and
Sobelow all passed. Focused `mix test.json` on
`antigravity_test.exs` + `model_availability_test.exs` after the
allowlist-doc cleanup. Result recorded in `.harness/audit.json`
`cold_check`.
