---
sha: 89e5c6132fbfe4399e9f62273d951ca6bd180971
short_sha: 89e5c61
audited_at: 2026-06-05
auditor_model: grok
verdict: 4 findings, 4 fixed forward
---

# Audit: 89e5c61 (range 61f207c..89e5c61)

Post-merge hygiene pass over 8 landed commits (Task 228: reviewer mechanical fallbacks —
malformed re-prompt generalization + reviewer timeout rotation + rotation slate + persisted
counts; delivery + tests + schema/migration; same-wave repair for recovering rename + LogRecord
credo; config: concurrency_cap 10→3; roadmap: task 228 done marker + 233 amend for
stale-base/orchestrator divergence + reviewer pool).

## Reviewed

- **Dead code / orphan paths** — grepped for `select_reviewer/1` (the pre-228 name) in `lib/`
  and `test/` (excluding roadmap). Only historical comments, the rename note in recovering/3,
  and old CHANGELOG prose remain. The repair commit (379054c) fixed the one live call site
  (recovering seam). No dead `fail_review_stuck` paths or duplicated settle clauses.
- **Bare `TODO` debt** — none in the changed files (run.ex, log_record, result, review, tests,
  stores, config, migration). All temporary notes carry `TODO(Task N)`.
- **CHANGELOG coverage** — the core 228 delivery (re-prompt generalization, rotation,
  `reviewer_*_count` facts, select_reviewers/1, test doubles, persisted coverage) and the
  supporting config/repair had no Unreleased entry (prior audit 61f207c covered 229). Filled.
- **Stale docs** — `lib/harness/run/review.ex` @moduledoc still described the pre-228
  distinction (malformed → immediate stuck; missing → re-prompt). `docs/agent-gate-workflow.md`
  had the same outdated routing bullets and a reviewer-selection sentence that said "first
  dispatchable ... (alphabetical)" ignoring the new slate + rotation + rejection-rate
  prioritize. Fixed both.
- **Formatting / edit artifact** — in the 97ae0a8 impl, the one-line fallback clause for
  `rotate_or_fail_review/2` was followed immediately by the next `@spec` with no blank line
  (inconsistent with surrounding). Fixed.
- **Elixir / project conventions** — new/edited fns in run.ex carry @spec (select_reviewers,
  auto_*, resolve_*, rotate_or_fail, etc.). All private. @moduledoc on Review updated for
  accuracy. LogRecord 34-field disable has the required mantra comment (added in repair).
  Test doubles (HangingAdapter, SpawnThenIdleReviewer) use @moduledoc false + full
  @behaviour + @impl. No @tag :skip or disabled checks.
- **Naming / symmetry** — `reviewer_reprompt_count` / `reviewer_rotation_count` are parallel,
  carried through Result, LogRecord, schema, postgres upsert (GREATEST like iterations),
  codec, contract, and exposed on the Result for KPI. `reviewer_candidates` field and
  `select_reviewers` returning `[primary | rest]` are consistent with route/rotate paths.
- **No leftover debug** — rotation and reprompt paths use `Logger.info` (visible in test
  runs); no IO.inspect, dbg, or stray puts in landed code.
- **Roadmap** — two commits in range touch `roadmap/tasks.toml` / `data.json` (task 228 done
  marker; 233 amend). Left completely untouched per operational rules.
- **Checks** — `mix format --check-formatted`, `mix compile --warnings-as-errors`,
  `mix credo --strict` (no issues), `mix doctor` (100% all), `mix test.json --cover
  --cover-threshold 80 --exclude integration` (1519 passed, 81.11% > 80, threshold_met) all
  green. Targeted rotation/re-prompt tests exercised the new paths (including log lines for
  rotate). Sobelow and dialyzer not re-run (no new attack surface or types from hygiene fixes;
  pre-existing in gate).
- **Reviewer re-prompt / rotation in context of 233** — the concurrency_cap drop and 233
  amend (stale-base + shared reviewer pool) are the operational motivation for the rotation
  fallback; the mechanical nature keeps judgment in agents.

## Found & fixed (4)

1. **CHANGELOG gap for Task 228 + wave.** Added detailed Unreleased entry under Added
   covering the mechanical fallbacks, the select_reviewers/rotate contract, the two
   witnessed counts + persistence, test strategy, the rename repair, the LogRecord credo,
   and the dev config cap change with its stale-base rationale.
2. **Stale routing description in Review moduledoc.** Updated to say "An unreadable artifact
   (missing or malformed) is re-prompted once ... before failing as `{:review_stuck, ...}`
   on a second miss (Task 203 generalized by Task 228)".
3. **Stale workflow doc.** Updated the harness-reads bullets in agent-gate-workflow.md to
   document unreadable → re-prompt, timeout → rotate, and the selection rules (ordered
   slate, prioritize by rejection rate, explicit list support).
4. **Missing blank line (impl edit hygiene).** Added blank line in run.ex between the
   one-line `rotate_or_fail_review` fallback clause and the following `@spec fail_review_stuck`
   (matches project style and every other fn).

## Reviewer false-rejection note

No reviewer rejections recorded for this project.

## Verdict

The range delivered the Task 228 mechanical fallbacks exactly per the agent-gate mantra:
harness counts (reprompt/rotation bounded by construction, witnesses the facts) and wires
the repeat/rotate; the reviewer AI still owns all judgment and the final write of a valid
artifact. The rename repair inside the wave and the concurrency tweak for 233 are
appropriate. Hygiene items were the usual post-delivery drift (changelog, docs now
reflecting the generalized unreadable + rotation model, one formatting artifact). All
fixed forward with zero behavior change. Checks green (cover holds). Range clean after
fixes.
