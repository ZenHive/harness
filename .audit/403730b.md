# Audit Report: 403730b

**Audit range (landed, already merged to development):**  
403730b (roadmap: task 258 -> in_progress) through 1af35d4 (harness: agent delivery — task 249 KPI review_stuck by cause) inclusive.

**Commits in chronological order (oldest → newest in the batch):**  
- 1af35d4 harness: agent delivery — task 249 KPI: surface review_stuck by cause + count selection-time stuck (nil reviewer_adapter) as orchestration health  
- 6a90ea5 roadmap: task 249 -> done (shipped 1af35d4e8a62)  
- c4e991e harness: agent delivery — task 251 Discovery-filing stage: implementer/reviewer/audit agents file follow-ups via rmap new, not lose them or bury them in TODO comments  
- 1cfb1d2 roadmap: task 251 -> done (shipped c4e991e4511a)  
- 360dc65 deps: bump descripex ~> 0.8 (typed MCP param schemas derived from @spec)  
- 9237a54 docs: recover-don't-redo + check-origin-before-not-landed (stale-local-checkout lesson)  
- 22c2ef5 roadmap: file task 259 — fix MCP param boundary once and for all (consumer-side; descripex 0.8.0 prereq met)  
- 454a846 roadmap: route task 259 to cursor on Opus (not claude) — rebalance per ledger  
- 278c8ef roadmap: route task 259 to cursor/composer-2.5 (mechanical task, no frontier model needed)  
- c963ab2 roadmap: task 255 -> in_progress  
- 403730b roadmap: task 258 -> in_progress  

**What was reviewed:**  
- Full diffs + context for every commit in the range (git show + surrounding code).  
- Substantive changes: the two agent-delivery landing commits (c4e991e test+fake support for rmap reachability; 1af35d4 test addition for direct tuple review_stuck causes), the docs lesson commit (9237a54), the descripex dep bump (360dc65), task 259 filing + routing markers (22c2ef5 and the two route notes), and the status markers.  
- Integration points in current tree: `lib/harness/rmap_path.ex` (the Task 251 substrate), its callsites in `lib/harness/run.ex` (implementer + reviewer via `in_run_env` + `reviewer_invocation`) and `lib/harness/audit.ex` (auditor), `lib/harness/agent_kpi.ex` (`aggregate_review_stuck_causes`, `review_stuck_cause`/`stuck_cause`, the tuple vs legacy-string paths), the added tests in `test/harness/run_test.exs` + `test/harness/agent_kpi_test.exs` + cross-check in `test/harness/audit_test.exs`, CHANGELOG.md, CLAUDE.md, `priv/includes/harness-workflow.md`, and the generated AGENTS.md.  
- Project conventions: @spec on every function (def + defp), concise @moduledoc / one-line @doc where name insufficient, functional style, doctests vs ExUnit boundaries, no bare TODO, no IO in @doc examples, tagged-tuple errors where applicable, minimal diff discipline.  
- Hygiene surfaces: dead code, debug output, stale comments, naming consistency, doc drift, CHANGELOG completeness, roadmap marker vs real work separation, reviewer-quality feedback items supplied for the range.  
- Prior audit precedent in the same lineage (e.g. 468c0b6 "clean (roadmap markers only...)").

**What was found:**  
- **CHANGELOG:** Accurate, complete entries already present under [Unreleased] for both Task 249 (review_stuck by cause + selection-time nil-reviewer_adapter orchestration health) and Task 251 (RmapPath + discovery filing instructions + operational rules update). No gaps to fill. The entries correctly call out the mechanical substrate nature, the prompts updated for agents, the test coverage, and the "harness decides nothing, reads nothing back" contract.  
- **Code (RmapPath + integration):** Clean new module. Concise moduledoc explaining the "makes reachable, does not interpret" intent. @spec on the public `ensure_agent_env/1` (marked @doc false as internal) and on all private helpers. Uses pattern matching for effective_path, pure functions, Enum pipelines. Falls back correctly to System.get_env when PATH absent/scrubbed. Filters to only dirs that actually contain the `rmap` binary at call time. Called for implementer, reviewer (via shared `in_run_env` + `reviewer_invocation`), and audit paths — full coverage of the three agent roles named in the task. No debug, no side effects, no over-refactoring.  
- **AgentKPI review_stuck aggregation:** The `aggregate_review_stuck_causes/1` + helpers correctly handle the modern direct-tuple detail form `{:review_stuck, {:reviewer_unavailable, _}}` (and siblings) by taking `elem(0)`, plus legacy string-report parsing for historical records, plus direct atoms for driver/timed_out/cancelled/other. The new test "counts direct persisted detail tuple tags" (added in 1af35d4) exercises the selection-time (nil reviewer_adapter) and crashed cases that the prior string-only path missed. Attribution remains pure counting of facts already written by harness; no new judgment.  
- **Tests added in range:** Real behavior under the documented threat model (scrubbed PATH env for the agent process inside the isolated worktree). The implementer test (c4e991e) + parallel reviewer test + audit test (pre-existing in the 251 work) use a temp executable fake rmap, app-env injection of candidate dir, and post-settle `git show` of the capture file written by the FakeAdapter. Edge (empty PATH, /usr/bin:/bin only) is covered. Not trivial assertions; they would have caught a missing ensure call on any of the three phases.  
- **Docs (9237a54):** The recover-don't-redo + check-origin lesson is high-signal operational hygiene. It cites the exact observed 2026-06-12 incident (runs 246/249/251 double-land risk from stale local tasks.toml), gives a clear decision table for recover primitives vs reset-to-pending, and emphasizes `git fetch origin && rebase` (or equivalent result_store / log checks) before mutating roadmap state. Added to the two canonical sources (CLAUDE.md for the harness repo, priv/includes/harness-workflow.md for portfolio sync). AGENTS.md was added as the generated companion (header correctly marks it auto-generated from CLAUDE.md via the marketplace sync script; do not hand-edit). Matches the "always update project documentation" and "document external inspirations / lessons" rules. No drift introduced.  
- **Dep + roadmap markers:** 360dc65 is a clean ~> 0.8 bump (descripex prereq for typed MCP consumer-side). 22c2ef5 + the two route notes + status markers (255/258/259) are the expected mechanical churn for filing + routing a small follow-up task (MCP param boundary, consumer side only). No code impact; prior audits treat pure marker batches as clean when the referenced deliveries were already hygiene-audited.  
- **Reviewer-quality feedback loop:** The single supplied item (task 208, run-1780839809032-86dd1f20: reviewer ran checks, coverage came in at 79.47% < 80% threshold on precommit.full, rescue-pass review only, no impl changes) is outside this range. No rejections or false-rejection signals for the 249/251 deliveries or the 255/258/259 planning work in the supplied list.  
- **Other hygiene (dead code, debug, conventions, naming, TODOs, etc.):** Nothing. No leftover `IO.inspect`, `dbg`, `TODO` without (Task N), commented-out code, stale cross-refs, or naming drift (RmapPath/ensure_agent_env/in_run_env align with existing GH_CONFIG_DIR scrubbing pattern). Duplication of the tiny `fake_rmap_dir` + `with_rmap_path_dirs` test helpers between run_test.exs and audit_test.exs follows the pre-existing per-suite pattern for narrow fixture needs; not a new smell introduced by this range. No scope expansion or refactors performed by the landed work.

**What was fixed:** none (range is clean; "finding nothing is a valid outcome").

**Discovery filings:** none.  
No follow-up tech debt, orphaned path, uncovered edge, or deferred decision surfaced during the review that justified a new rmap task. The recover-don't-redo lesson is already durably recorded in the canonical workflow docs (the correct place). The rmap reachability + KPI cause surfacing work is complete and tested for the three agent roles. Task 259 itself is the vehicle for the next MCP boundary fix.

**Summary for next auditor:** This batch was two small, well-scoped agent-delivery finalizers (test + support only; core substrate landed with the feature), one high-value operational lesson doc, a routine typed-dep bump, and the usual roadmap status/route/file markers. All surfaces that matter for hygiene (CHANGELOG, specs/docs, test quality, convention adherence, integration completeness for implementer/reviewer/audit, reviewer feedback context) were sound. No fixes or new tasks filed. The audit marker commit (this one) follows the prior pattern.

(End of report — commit subject will be the machine-readable marker for the next range.)
